// Prism bundle for regex-pattern highlighting pages (issue #1669).
// Import order matters: prismjs core sets the global Prism object each
// component below extends -- see components.json for the dependency graph.
import 'prismjs';
import 'prismjs/components/prism-regex';
