---
name: delegate
description: Run one substantial ad-hoc pfBlockerNG coding task through the brief, implementation, and independent-gate contract. Use for "delegate this" or "do this via the contract".
---

# Delegate a task

Read `../../../.claude/skills/delegate/SKILL.md` for the complete contract. Replace its
`Workflow({name: 'phase-step', ...})` call with `$phase-step`. Use a dedicated
worktree and Codex `planner`, `implementer`, and `adversarial-reviewer` roles;
the parent validates each artifact and handles landing.
