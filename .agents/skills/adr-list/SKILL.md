---
name: adr-list
description: List pfBlockerNG ADRs that are open, unimplemented, and not already in an open PR. Use for "list ADRs" or "what ADRs can I work on next".
---

# List open ADRs

Follow `../../../.claude/skills/adr-list/SKILL.md` and its canonical `CLAUDE.md` policy;
use `AGENTS.md` only to translate runtime-specific surfaces. Remain read-only.
Verify both whether an ADR landed on `devel` and whether it already has an open
PR before listing it as available.
