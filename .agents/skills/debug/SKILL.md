---
name: debug
description: Debug a pfBlockerNG symptom or failing command with a structured hypothesis ledger. Use for "debug this", "why is X failing", or "investigate this error".
---

# Debug with evidence

Read `../../../.claude/skills/debug/SKILL.md` for the detailed ledger and reporting shape.
Reproduce first, name at least two hypotheses, run discriminating probes, and edit
only after confirming the cause. Apply the canonical `CLAUDE.md` policy through
the Codex runtime mapping in `AGENTS.md`.
