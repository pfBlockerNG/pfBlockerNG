# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.venv
venv
.mypy_cache/
.benchmarks/

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Editor temporaries ────────────────────────────────────────────────────────
*.swp
*.swo
*~

# ── PHP ───────────────────────────────────────────────────────────────────────
vendor
.phpunit.cache/
.phpunit.result.cache

# ── Python coverage (pytest-cov / coverage.py; issue #38) ─────────────────────
.coverage
.coverage.*
coverage.xml
coverage.json
htmlcov/

# ── Shell tests (shellspec / kcov) ────────────────────────────────────────────
coverage/
.shellspec-quick.log
report/

# ── Build / release artefacts ─────────────────────────────────────────────────
*.tar.gz
*.tar.bz2
*.zip

# ── ADR-14 Tier-B browser screenshots (build output, human-review artifacts) ──
test-results/

# ── AI-agent worktrees (per-task, never committed) ───────────────────────────
.claude/worktrees/
