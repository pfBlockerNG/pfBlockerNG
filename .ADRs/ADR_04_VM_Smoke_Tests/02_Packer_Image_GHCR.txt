================================================================================
PHASE 2 PROMPT — Packer template + image-build workflow → GHCR
Target model: Claude Sonnet 4.6
ADR: .ADRs/ADR_04_VM_Smoke_Tests/ADR.md   (read §2 "Image build"/"Image store"
     rows, §3 redistribution risk, §7 reject criteria FIRST)
================================================================================

ROLE
You are on the `adr/04` branch (off `devel`) performing Phase 2 of ADR-04. Produce a
version-tagged pfSense CE qcow2 and publish it to PRIVATE GHCR via oras. The image
build sits behind ONE output contract — the published qcow2 — so HOW it is built is
swappable: (a) BASE OS install and (b) PROVISIONING. Automate each with Packer WHERE
FEASIBLE, but the base install may be a manual seed and — per §7 — if Packer cannot
drive the build AT ALL, the maintainer PROVIDES A READY-MADE qcow2 WHOLESALE, built by
hand per a documented runbook. All three modes are accepted. The phase's HARD
deliverables are: the publish pipeline (oras -> private GHCR, versioned), a maintainer
RUNBOOK of exactly what is baked, and a VERIFICATION that the published image passes the
Phase-1 round-trip — NOT a working Packer template. Do NOT burn this phase fighting
fragile installer keystrokes or Packer. Work inline on `adr/04`, one commit, push
directly.

WHY THIS PHASE EXISTS
Phase 1 proved a GH-hosted runner can boot+drive a hand-built pfSense VM. This phase
makes the image reproducible + refreshable on a CE bump and publishes it to GHCR so
the test workflow (Phase 6) just pulls it. Fully unattended pfSense CE install is
unofficial, fragile, and version-sensitive (installerconfig/rc.local media surgery, or
boot_command keystrokes) — so it is NOT a prerequisite. The seam is the published
qcow2: everything downstream consumes only `oras pull`, so the base OS can be installed
by hand once per CE version while the bakeable provisioning stays scripted and
reproducible. CE ONLY — pfSense Plus is a licensing nightmare, out of scope. The image
is PRIVATE in GHCR (Netgate trademark/binary redistribution — see §3).

REQUIRED READING (inspect before editing)
- FIRST, read the prior phase's handoff:
  .ADRs/ADR_04_VM_Smoke_Tests/RESULTS/01_Results.txt — it must say GO (and give the
  working qemu/hostfwd/SSH model + what's baked into the spike image). If it says
  NO-GO or is missing, STOP and report: Phase 2 must target the recorded pivot
  (self-hosted) or not proceed.
- .ADRs/ADR_04_VM_Smoke_Tests/ADR.md  (§2 Image build/store/versioning; §5 docs note;
  §7 reject: "unattended CE install not reproducible" and "redistribution blocked").
- scripts/deploy.sh + src/usr/local/www/pfblockerng/pfblockerng.php (so the baked-in
  base config + installed pfBlockerNG-devel match what the harness will drive).
- CLAUDE.md (.editorconfig for HCL/YAML indent; commit style; CE-version-bump docs).

ACTION PLAN (ordered)
1. BASE OS — CANONICAL mode is "seed once, upgrade in place":
   - SEED (manual, once): a documented runbook for the maintainer to do ONE clean
     manual pfSense CE install -> archive it as the immutable seed qcow2. This is the
     reproducibility anchor; never overwrite it. In practice the seed is the Proxmox VM's
     disk exported/converted to qcow2 (`qemu-img convert`); the runbook MUST record the
     source VM's HARDWARE PROFILE — machine type, scsihw/disk bus, virtio-net model + the
     pinned MAC (`BC:24:11:37:9C:AC`), cpu/cores/mem — so the boot helper mirrors it and
     pfSense doesn't re-detect hardware (NIC type/MAC/count changes break the boot).
   - VERSION BUMP (scripted, default path): boot the prior published image, run the
     pfSense upgrader over SSH (`pfSense-upgrade` — the CLI behind System > Update;
     confirm the exact non-interactive flags during the spike, don't guess), wait on a
     readiness signal (upgrade done + reboot + services up, NOT a fixed sleep),
     re-snapshot. RE-BASELINE with a fresh manual seed on a MAJOR version jump (the
     upgrader is riskiest there and cruft matters); upgrade-in-place for patch/minor.
   - OPTIONAL (spike-gated, NOT required): automate the clean install with Packer
     from ISO (installerconfig/rc.local surgery or boot_command keystrokes). Time-box
     it; if fragile, the manual seed already covers it — MOVE ON, do not rabbit-hole.
   - In all cases the Packer QEMU builder consumes the base via `disk_image = true`
     (boot existing qcow2, no ISO) and runs only the provisioning in step 2.
2. PROVISIONING — automate with Packer (qemu builder, disk_image=true) WHERE FEASIBLE;
   else the maintainer bakes these by hand into the qcow2 per the runbook. Either way the
   image MUST contain, idempotently:
   - a TEST SSH public key (from secret `SMOKE_SSH_PUB_KEY`) into root's authorized_keys
     + root SSH enabled — the harness connects with the matching private key
     (`SMOKE_SSH_PRIV_KEY`);
   - do NOT bake pfBlockerNG — the image is clean pfSense CE; the harness installs the
     branch from src/ after boot via scripts/install-from-repo.sh (the port's
     `php -f /etc/rc.packages <pkg> POST-INSTALL` hook — all local, no internet). The
     image is therefore pfBlockerNG-version-agnostic and reused across the per-minor
     CE matrix;
   - a known base config.xml (interfaces, services up) with the `admin` password set to
     match secret `SMOKE_ADMIN_PASSWORD` (store its bcrypt hash in config.xml);
   - Unbound `local-data` for the hermetic control domain(s) named in 01_Results.txt;
   - serial console retained for CI boot logs.
3. OUTPUT: a qcow2. Keep it small (compact/zero free space) — it travels over GHCR.
4. BUILD WORKFLOW (`.github/workflows/build-image.yml`): triggers
   `workflow_dispatch` (inputs: target PFSENSE_CE_VERSION, and base-source = seed |
   prior-version-to-upgrade) + `schedule`. Steps: ensure /dev/kvm; install packer +
   qemu + oras; `oras pull` the chosen base (seed or prior snapshot); if upgrading, run
   the pfSense-upgrade step (step 1) before provisioning; run `packer build`
   (provisioning via disk_image=true) IF Packer is used, ELSE accept a maintainer-provided
   qcow2 as the build input (skip build entirely); `oras push` the result to
   ghcr.io/<org>/<repo>/pfsense-ce
   tagged `:<PFSENSE_CE_VERSION>`. **RETAIN the seed and every version snapshot — never
   overwrite** (recoverability + audit; ADR §2 versioning). Use GITHUB_TOKEN with
   packages:write; the package is PRIVATE.
5. PROVE IT: after push, a verification step (or reuse the Phase 1 spike workflow)
   pulls the freshly published image and runs the Phase 1 round-trip — the
   reproducible image must boot + pass the same SSH/pfctl/dig/php-update checks.

CONSTRAINTS
- CE ONLY. No pfSense Plus. Single latest-stable version (no broad matrix).
- GHCR package PRIVATE. Do not publish the image publicly. Do not commit the SSH
  private key, ISO credentials, or any secret — use Actions secrets.
- Do NOT touch `src/`. Do NOT add to the default pytest run.
- HCL/YAML indent per .editorconfig; any shell is POSIX sh, ShellCheck-clean.
- Neither a non-automatable installer NOR non-automatable provisioning is a reject —
  fall back to a maintainer-built base, hand-baked provisioning, or a READY-MADE qcow2
  PROVIDED WHOLESALE (§7); keep the GHCR publish + verification automated and write the
  runbook. Only a qcow2 that can't be made bootable + round-trip-passing AT ALL is a
  reject (§7) — Phase 1 already disproved that. Never fake a green.

VERIFICATION (must all pass before the phase is done)
- `packer build` produces a qcow2 (locally or in CI); `oras push` lands a PRIVATE
  GHCR artifact tagged by CE version.
- The published image, pulled fresh, BOOTS and passes the Phase 1 round-trip
  (SSH + pfctl -sr + php pfblockerng.php update + dig local-data name).
- `python -m pytest` → green (unchanged).  `ruff`/ShellCheck → clean.

EXPECTED RESULT
A private, version-tagged pfSense CE qcow2 in GHCR — produced by Packer where feasible
or provided wholesale by the maintainer — with an automated publish + verification
pipeline and a runbook of what is baked, verified to boot and pass the control round-trip.

COMMIT (single)
    ci: add pfSense CE image publish-to-GHCR pipeline + build runbook (ADR-04 P2)
Push directly to `adr/04`; PR only if rejected. PR body via --body-file.

HANDOFF — write `.ADRs/ADR_04_VM_Smoke_Tests/RESULTS/02_Results.txt` (plain .txt)
For Phase 3 (pytest scaffolding). Include: whether the qcow2 was Packer-built or PROVIDED
WHOLESALE by the maintainer, and the path to the build/bake runbook; the base-OS strategy
as built — the manual SEED runbook (clean install), the scripted UPGRADE-IN-PLACE step +
the exact `pfSense-upgrade` non-interactive flags + readiness signal used, and the
re-baseline-on-MAJOR rule; where the seed + each version snapshot are archived in
GHCR; the GHCR ref + tag scheme and exact
`oras pull` command; PFSENSE_CE_VERSION used; what is baked into the image (SSH user
+ key secret name, pfBlockerNG-devel path, base config specifics, the exact
local-data control name(s) + their expected answers, DNS/SSH listen setup, AND the
source-VM HARDWARE PROFILE — machine type, scsihw/disk bus, virtio-net model + the pinned
MAC (`BC:24:11:37:9C:AC`), cpu/cores/mem — the boot helper must mirror, plus how the Proxmox disk
was exported to qcow2); the
qcow2 size + boot-to-ready from the published image; how to rebuild on a CE bump; any
install-automation gotchas; and confirmation the published image passes the Phase 1
round-trip. If reject criteria were hit, say so plainly and stop.
