================================================================================
PHASE 5 PROMPT — GUI rollback panel + landing "Older releases"
Target model: Claude Sonnet 4.6
ADR: .ADRs/ADR_27_Release_Rollback_And_EOL_Routing/ADR.md   (read §2.1 GUI/landing rows, §2.3, §6 Phase 5)
================================================================================

ROLE
Branch adr/27-release-rollback-and-eol-rou (off devel). Phase 5 of 5 (final). Ships USER-FACING surface:
a landing-page "Older releases" view + a pfSense GUI rollback page. Inline, one commit, push directly.
This touches src/ — the ADR-14 ui_render gate applies.

WHY THIS PHASE EXISTS
With the catalog now retaining releases (Phases 1–4), surface the rollback targets to humans: on the
self-hosted landing page (parallel to "Older nightlies") and in the pfSense GUI (the rollback slice of
the deferred ADR-19 panel — see ADR §2.3).

REQUIRED READING
- FIRST, read RESULTS/04_Results.txt — confirm rollback works via the catalog + the exact pkg command
  the GUI must run. If it does NOT (REJECT trigger fired), STOP and report — the GUI cannot ship a
  rollback that pkg can't perform.
- .ADRs/ADR_27_Release_Rollback_And_EOL_Routing/ADR.md  (§2.1 GUI + landing rows, §3 GUI risk, §7)
- scripts/gen_landing.py — _older_nightlies_by_edition / _older_nightlies_details / _packages_html
  (mirror these for "Older releases"); tests/test_gen_landing.py.
- src/usr/local/www/ — an existing pfBlockerNG page for the layout/idioms; src/usr/local/pkg/pfblockerng/
  for the install-helper boundary; CLAUDE.md (PHP standards, help-text style, PFBL-01).
- tests/smoke/ui/ + docs/misc/architecture-notes.md (ADR-14 ui_render tier).

ACTION PLAN
1. gen_landing.py: add per-edition "Older releases" disclosure (retained devel/stable, minus the
   newest already in the edition table), reusing the matrix-join + _older_nightlies_table_html shape.
   Unit tests in tests/test_gen_landing.py (split per edition; excludes the newest; columns).
2. src/usr/local/www/: a version-rollback page — list INSTALLED version + AVAILABLE versions (read the
   live repo catalog via pkg rquery/pkg search; do NOT hardcode), with a guarded "switch to version X"
   POST that runs the pinned `pkg install <name>-<version>` from RESULTS/04. Warn on downgrade across a
   config-schema change (ADR §3 foot-gun); no die()/exit() in library code; PFBL-01 where input applies.
3. Wire the page into the pfBlockerNG menu the same way sibling pages are registered.
4. Tests: gen_landing unit cases; tests/smoke/ui -m ui_render for the new page (200, no
   Fatal/Parse/Warning/Notice/Uncaught, a page marker, no new php_error.log line).

CONSTRAINTS
- PHP 8.3, tabs, pfSense GUI idioms + help-text style; POSIX sh if any shell.
- The switch action MUST be guarded (confirm + downgrade warning) — never a one-click silent downgrade.
- Do NOT build the full ADR-19 channel panel — only the rollback/version-pick slice.

VERIFICATION (must all pass)
- python -m pytest / ruff / mypy tests/   → green/clean (gen_landing unit cases)
- php -l on touched PHP; PFBL-01 (vendor/bin/phpcs) where applicable
- npx markdownlint-cli2 if docs touched
- Live (dispatch): tests/smoke/ui -m ui_render — the rollback page renders clean on the VM.

EXPECTED RESULT
Landing page shows "Older releases" per edition; the pfSense GUI offers a guarded version rollback that
performs the proven pkg command.

COMMIT (single)
    pfblockerng: add release rollback GUI + landing older-releases view (ADR-27 P5)
Push directly to the branch; PR only if the push is rejected.

HANDOFF — write RESULTS/05_Results.txt
State: the page route + menu entry; the guard/warning behaviour; the gen_landing additions + tests; the
ui_render result. Note Part 2 (§2.4) remains design-only/deferred. ADR-27 Part 1 complete — summarize
the Definition-of-Done evidence (§7) for the maintainer's accept decision.
