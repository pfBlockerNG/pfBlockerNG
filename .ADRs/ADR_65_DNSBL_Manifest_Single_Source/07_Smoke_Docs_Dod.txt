================================================================================
PHASE 7 PROMPT -- Smoke, docs, ADR-06 amendment, Definition of Done (behaviour-preserving)
Target model: Sonnet 5 implementer under an orchestrator gate (xhigh adversarial review)
ADR: .ADRs/ADR_65_DNSBL_Manifest_Single_Source/ADR.md   (read SS7 Definition of done)
================================================================================

ROLE
You are the IMPLEMENTER. Work in the per-ADR worktree on branch
adr/65-dnsbl-manifest-single-source. Implement yourself; never spawn sub-agents. Inline, one commit,
push directly. Test mode: behaviour-preserving -- this phase adds live-VM smoke coverage + docs + an
ADR amendment; it changes no production behaviour (the smoke rows OBSERVE the Phase 2-6 behaviour,
they do not create it).
Blast-radius note for the gate: NONE (production). New smoke tests + docs + ADR text only; no src/
runtime change. The smoke rows are the live-VM proof CI cannot run.

WHY THIS PHASE EXISTS
The module answers a real query only on a live VM; the fail-loud + reports-from-logs + hit-via-query
behaviours are all live-VM observable. This phase lands that coverage on the CE + Plus fan-out,
records the retirement in ADR-06 and the pipeline doc, and closes #1244/#1245.

REQUIRED READING
- FIRST, read RESULTS/06_Results.txt (and skim 02/04/05 for the behaviours to smoke). If missing,
  STOP and report.
- .ADRs/ADR_65_DNSBL_Manifest_Single_Source/ADR.md -- SS7 Definition of done (the smoke checklist +
  reject criteria) and the D1-D4 deltas.
- .ADRs/ADR_06_DNSBL_Preprocessing_To_Python/ADR.md -- add a SS8 post-merge amendment recording:
  manifest = single DNSBL source of truth, the .txt fallback retired, fail-loud contract, reports
  render block-time attribution (no right-now re-check), the query channel.
- docs/misc/alerts-reports-pipeline.md -- rewrite to the ONE-phase (log-driven) attribution model;
  docs/misc/architecture-notes.md -- update the DNSBL/manifest section.
- tests/smoke/ + tests/smoke/ui/ -- CLAUDE.md "Smoke tests" section; mirror an existing smoke module
  (probe ON-BOX via drill @127.0.0.1, block = NOERROR+VIP or NULL, unique_domain() fixtures, drive
  the guest via /bin/sh).
- CLAUDE.md -- Smoke tests section, Test coverage mandate #4 (Tier-A), ADR acceptance rule.

REALITY OVERRIDE (standing rule)
Line numbers and code-state claims predate implementation; the prior RESULTS files and the live tree
override this prompt on any conflict -- verify before acting and flag contradictions in the handoff.
Run the live-VM smoke on a leased PFB_BOXES box; a smoke row is EXECUTED on a box, never dispatched
to CI and claimed, never reasoned through.

ACTION PLAN (ordered)
1. Smoke rows (executed on a leased box, CE + Plus):
   (a) a uuid-domain feed match blocks on-box AND a pfb_dnsbl_query of the same name returns the
       matching verdict while adding ZERO dnsbl.log lines and bumping NO counter (D4 / Semantic 3);
   (b) manifest removed -> empty DNSBL + file_notice + widget out-of-sync + NO stale block, then a
       Force reload self-heals (D3);
   (c) a block-page (VIP) hit increments the widget group counter with the query-derived group (D4);
   (d) a reports/alerts DNSBL row renders the logged feed/group with the re-check gated off (D2).
2. Tier-A ui_render rows for Alerts / Reports / Unified (already touched in Phase 4 -- confirm green
   here on the box).
3. Docs: rewrite alerts-reports-pipeline.md to one-phase; update architecture-notes; amend ADR-06
   SS8; add release-note text for D2 (refinement badges gone, feed-filter matches logged feed).
4. Close #1244 and #1245 with a comment pointing at this ADR; FILE the follow-up tracking issue for
   retiring dnsblcache + the pfb_dnsbl_parse grep helpers (out of scope here, SS"Explicitly kept").

CONSTRAINTS
- Smoke fixtures: unique_domain() / RFC-5737/3849 only; never RFC 6761 TLDs or HSTS-preload names
  (except a case specifically testing HSTS). Probe ON-BOX, first response authoritative.
- Docs are documentation-class (normal grammar); markdownlint clean.
- ADR-06 amendment is text-only under SS8; do not rewrite ADR-06's decision body.

VERIFICATION (executed -- paste output)
- test mode: behaviour-preserving smoke + docs -- the new smoke rows GREEN on a leased box for BOTH
  CE and Plus (paste the `scripts/local-smoke.sh` invocations + result tails). This is the ADR's
  acceptance evidence (SS7).
- Tier-A `ui_render` green on the box (paste).
- `npx markdownlint-cli2` on the changed docs + the ADR-06 amendment.
- `scripts/agent/run-gates.sh --diff origin/devel`.
- Confirm #1244/#1245 closed and the follow-up issue filed (paste the URLs).

EXPECTED RESULT
Live-VM CE + Plus proof of the query channel, fail-loud, reports-from-logs and hit-via-query
behaviours; ADR-06 amended; the pipeline doc reflects the one-phase model; #1244/#1245 closed;
ADR-65 ready to flip to Accepted on the green fan-out.

COMMIT (single)
    pfblockerng: ADR-65 smoke + docs + ADR-06 amendment; close #1244/#1245 (ADR-65 P7)
Push directly to adr/65-dnsbl-manifest-single-source; open a PR only if the direct push is rejected.

HANDOFF -- write .ADRs/ADR_65_DNSBL_Manifest_Single_Source/RESULTS/07_Results.txt with the fixed
fields: Verdict; What changed (files + one-line why + commit hash); Gates (commands + pasted tails);
the CE + Plus smoke evidence (each row's invocation + result); the Tier-A ui_render result; the docs
+ ADR-06 amendment confirmation; the #1244/#1245-closed + follow-up-issue URLs; Coverage-matrix
(D1-D4 each proven on the box); Deviations / judgment calls (or "none"); the final DoD checklist
state.
