================================================================================
PHASE 4 PROMPT — Uniform timestamp for the Python-side logs
Target model: Claude Sonnet 5
ADR: .ADRs/ADR_60_Age_Based_Log_Retention/ADR.md   (read §1.3 dnslog/dnsreplylog row, §6 Phase 4)
================================================================================

ROLE
Inline on branch `adr/60-age-based-log-retention`. Phase 4 of 8. Behaviour-CHANGING. One commit,
push directly.

WHY THIS PHASE EXISTS
`dnslog`/`dnsreplylog` (written by `pfb_unbound.py`, inside Unbound's chroot) use `make_timestamp()`,
which has no year and can return `""` on a real (if rare) `TypeError`-twice path. This is the last
remaining no-year format after Phases 2-3; closing it makes the age-cutoff cutoff (Phase 5) uniform
across all 10 log types with a single parse pattern.

REQUIRED READING
- FIRST, read `.ADRs/ADR_60_Age_Based_Log_Retention/RESULTS/03_Results.txt` — confirm Phase 3 landed
  and its claim about `unilog`'s IP-row passthrough. If missing, STOP and report.
- ADR.md §1.3 (dnslog/dnsreplylog row), §6 Phase 4.
- src/usr/local/pkg/pfblockerng/pfb_unbound.py: `make_timestamp()` (~:2702-:2708), its 4 call sites
  (~:2617, ~:2650, ~:2680 — all writing `dnsbl.log`; ~:2962 — writing `dns_reply.log`), the
  `iso_timestamp()` helper Phase 1 added (unwired) near it.
- Grep every OTHER call site of `make_timestamp(` to make sure the 4 listed are exhaustive — do not
  trust this prompt's count, re-derive it (`grep -n "make_timestamp(" src/usr/local/pkg/pfblockerng/pfb_unbound.py`).

ACTION PLAN
1. Replace `make_timestamp()`'s body: `"%b %-d %H:%M:%S"` → `"%Y-%m-%d %H:%M:%S"`. Keep the
   `for _ in range(2): try/except TypeError` retry shape and the `""` fallback exactly as-is (that
   edge case is unrelated to the format and stays). You may now delete the separate unwired
   `iso_timestamp()` Phase 1 added and just use `make_timestamp()` directly (one function, not two) —
   OR keep `make_timestamp()` as the sole timestamp function and remove `iso_timestamp()` as dead code
   either way, don't leave two near-identical functions both live.
2. Re-verify (do not assume from the ADR text) that `unilog`'s python-row passthrough
   (`pfb_daemon_filterlog()` reading `dnsbl.log`/`dns_reply.log` via `tail_pfb -F`) needs no PHP-side
   change — it copies the CSV field verbatim, so the new format flows through automatically. If your
   verification finds otherwise, STOP and report the discrepancy rather than silently patching around
   it (this is exactly the kind of prior-phase claim CLAUDE.md says to disprove, not obey).

CONSTRAINTS
- Python: stdlib only — `pfb_unbound.py` runs inside Unbound's embedded loader (no `python3` symlink
  on the appliance; this file is the sole sh/php-exception). `datetime`/`time` only, already the case.
- Type-hint the function; keep `from __future__ import annotations` if already present in the file.
- Do not touch the CSV field construction/ordering — only the timestamp string's format.
- Line numbers and code-state claims in this prompt were written before earlier phases ran; the
  prior RESULTS files and the live tree override this prompt on any conflict — verify each claim
  before acting on it, and flag contradictions loudly in the handoff (Phases 2-3 may have shifted
  nearby PHP lines, but this file is Python and likely unaffected — confirm, don't assume).

VERIFICATION
- `python -m pytest` full suite green.
- `ruff check .` / `ruff format --check .` clean.
- Red→green proof: Phase 1's baseline pytest oracle (pinning the old `"%b %-d %H:%M:%S"` no-year
  format) now FAILS against the new code in the expected direction (paste), new assertion (year
  present, `%Y-%m-%d %H:%M:%S`) passes (paste). The `TypeError`-twice → `""` fallback test still
  passes unchanged (paste — proves you didn't touch that path).
- A PHP-side `UnifiedFormatTest.php` case (or extend Phase 3's) confirming a synthetic python-sourced
  `unilog` row (constructed with the NEW dnsbl.log/dns_reply.log CSV shape) renders with the new
  year-bearing timestamp with zero PHP-side change required — this is the verification of the
  "no separate change needed" claim, not just an assertion of it.

EXPECTED RESULT
`dnslog`/`dnsreplylog`/`unilog`'s python-sourced rows all carry `%Y-%m-%d %H:%M:%S` timestamps. All
10 log types now share exactly one timestamp format and position (free-text: line-start prefix; CSV:
field 0/1 as before, just reformatted).

COMMIT (single)
    pfb_unbound: add year to DNSBL/DNS-reply log timestamps (ADR-60 P4)
Push directly to `adr/60-age-based-log-retention`; open a PR only if the direct push is rejected.

HANDOFF — write `.ADRs/ADR_60_Age_Based_Log_Retention/RESULTS/04_Results.txt` with: verdict; what
changed + commit; gates + pasted output; red→green proof; the unilog-passthrough verification result
(confirmed or discrepancy found); coverage-matrix ticks (all 10 log types' timestamp format now
uniform — list them); deviations (or "none"); carry-forward for Phase 5 (the age-cutoff cap can now
assume ONE format across all types).
