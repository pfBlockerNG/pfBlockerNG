================================================================================
PHASE 3 PROMPT — Smoke + Definition of Done
Target model: Claude Sonnet 4.6
ADR: .ADRs/ADR_22_DNSBL_Parser_Scheme_Anchored_Host/ADR.md
     (read §7 DoD + manual smoke checklist FIRST)
================================================================================

ROLE
You are on the `adr/22-dnsbl-parser-scheme-anchored-host` branch performing Phase 3
of ADR-22 — the final phase. This phase adds a live-VM smoke case, updates the inline
comment at the changed call site, and records the DoD evidence.

REQUIRED READING
- FIRST, read the prior phase's handoff:
  .ADRs/ADR_22_DNSBL_Parser_Scheme_Anchored_Host/RESULTS/02_Results.txt — confirm Phase 2
  is complete and all PHPUnit + Python tests pass. If missing, Phase 2 is not complete;
  STOP and report.
- .ADRs/ADR_22_DNSBL_Parser_Scheme_Anchored_Host/ADR.md — §7 DoD + manual smoke checklist.
- tests/smoke/test_smoke_feeds.py — the existing feed smoke structure.
- CLAUDE.md — "Smoke tests (ADR-04)" — `unique_domain()` mandatory; probe on-box via
  `drill @127.0.0.1`; first response is authoritative; block shape = NOERROR + VIP or
  NULL, NOT NXDOMAIN.

ACTION PLAN

1. UPDATE the inline comment at the scheme-strip call site in the non-lite block to
   replace the old `// If 'http|https|telnet|ftp://' found, remove` comment with:

   ```php
   // Strip scheme:// if present. Any valid RFC 3986 scheme is accepted
   // (http, https, ftp, telnet, evil, pkg+https, ...); an invalid scheme
   // (digit-start, empty, special chars) → pfb_parsed_fail() + skip (ADR-22).
   ```

2. ADD one `@pytest.mark.smoke` + `@pytest.mark.timeout(300)` test case to
   `tests/smoke/test_smoke_feeds.py`:

   `test_invalid_scheme_not_blocked`

   ```text
   Scenario: A feed line with an invalid RFC 3986 scheme is skipped; a line with a
   valid (but non-http) scheme is still blocked.

   Background: Custom_List feed containing:
     http://<unique-blocked>.<tld>       (http:// → valid, should be blocked)
     evil://<unique-evil>.<tld>          (evil:// → valid RFC 3986, should be blocked)
     123://<unique-skipped>.<tld>        (digit-start → invalid, should be skipped)

   Given: none of the three domains are in DNSBL.
     Assert BEFORE: drill @127.0.0.1 <blocked> → NOERROR (not blocked yet).
     Assert BEFORE: drill @127.0.0.1 <evil> → NOERROR (not blocked yet).
     Assert BEFORE: drill @127.0.0.1 <skipped> → NOERROR (not blocked yet).

   When: feed loaded; pfBlockerNG DNSBL update; Unbound reloads.

   Then:
     drill @127.0.0.1 <blocked> → sinkhole VIP or NULL (http:// still works).
     drill @127.0.0.1 <evil> → sinkhole VIP or NULL (valid custom scheme still works).
     drill @127.0.0.1 <skipped> → NOERROR (123:// invalid → skipped, not blocked).
   ```

   Implementation notes:
   - `helpers.unique_domain()` for all three domains.
   - After update: `wait_unbound_ready` before asserting DNS.
   - Clean up in `finally`: remove custom list + re-run update.

3. DISPATCH smoke: `gh workflow run smoke.yml -f pytest_marker=smoke`. Capture run ID.

4. UPDATE the title in the ADR.md header to match the refined design. The file is already
   titled "DNSBL Parser Scheme-Validated Host Extraction" — confirm it matches.

5. RECORD DoD evidence in `RESULTS/03_Results.txt`:
   - Phase 1: helper definition, oracle tests, PHPUnit pass count.
   - Phase 2: tightened function body, call site update, PHPUnit pass count.
   - Phase 3: smoke run ID + per-case PASS; exact `drill` outputs.
   - Comment update: old text vs new text.
   - All CI gates: PASS.
   - Manual smoke checklist status (maintainer).

CONSTRAINTS
- `@pytest.mark.smoke` only (NOT `repo`).
- `unique_domain()` mandatory for every domain.
- VM left CLEAN in `finally`.

VERIFICATION (must all pass)
- `python -m pytest` (default, without smoke) → unchanged.
- `ruff check . && ruff format .` → clean.
- `mypy tests/` → clean.
- `php -l` → clean (comment change only).
- Smoke: new test GREEN; existing smoke GREEN.

EXPECTED RESULT
Live-VM evidence: `123://evil.com` is skipped (not blocked); `evil://evil.com` is still
blocked (valid scheme); `http://evil.com` still blocked (no regression).

COMMIT (single)
    tests: ADR-22 P3 smoke — RFC 3986 scheme validation (ADR-22 P3)
Push to `adr/22-dnsbl-parser-scheme-anchored-host`; update the PR.

HANDOFF — write `.ADRs/ADR_22_DNSBL_Parser_Scheme_Anchored_Host/RESULTS/03_Results.txt`
(plain .txt)
Record: smoke run ID + per-case PASS; exact `drill` outputs; manual smoke checklist
status; all CI gates PASS. Final handoff — ADR complete pending maintainer sign-off.
