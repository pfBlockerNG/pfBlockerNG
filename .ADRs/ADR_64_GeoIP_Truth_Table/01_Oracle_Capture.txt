================================================================================
PHASE 1 PROMPT -- Oracle: capture today's country/continent build (behaviour-preserving)
Target model: Sonnet 5 implementer under an orchestrator gate (xhigh adversarial review)
ADR: .ADRs/ADR_64_GeoIP_Truth_Table/ADR.md
================================================================================

ROLE
You are the IMPLEMENTER. Work in the per-ADR worktree on branch adr/64-geoip-truth-table (off the
latest devel; create if absent). You implement yourself with Read/Edit/Write/Bash -- you NEVER
spawn sub-agents. Phase 1 changes NO production code: it captures today's behaviour as golden fixtures.
Test mode: BEHAVIOUR-PRESERVING -- this phase is the ORACLE. No production file may change; the golden fixtures capture TODAY's output and become the red half of Phase 3.
Blast-radius note for the gate: NONE -- test/fixture-only phase. A production-file change in the diff is a phase failure.
Follow CLAUDE.md in full.

WHY THIS PHASE EXISTS
Phase 3 rewrites where country identity comes from. The ADR's load-bearing claim is that the
output changes ONLY by the enumerated deltas D1-D4. That claim is falsifiable only against a
captured baseline. Capture it BEFORE anything moves -- a baseline taken after the change proves
nothing (CLAUDE.md Test coverage #1).

REQUIRED READING
- .ADRs/ADR_64_GeoIP_Truth_Table/ADR.md IN FULL -- especially SS1.3/SS1.4 (the measured facts),
  SS2.1 (the table + its sources), SS2.2 (per-continent unknown buckets), SS2.3 (notices),
  "Accepted user-visible deltas" (D1-D4: the ONLY permitted output changes) and "Semantics that
  MUST be preserved".
- Issue #1235 IN FULL, including every comment (`gh issue view 1235 --comments`) -- the design
  record this ADR was distilled from. Read it, not just the ADR summary.
- CLAUDE.md in full -- PHP standards, Test coverage mandate, the delegation contract.
- src/usr/local/www/pfblockerng/pfblockerng.php -- pfblockerng_uc_countries() (~:855-1460),
  pfblockerng_get_countries(), $pfb_geoip_all (~:941-1191), $top_20 (~:883), the two hardcoded
  continent-level buckets (~:935-938), the Locations parse gate (~:898), the Blocks parse
  (~:1223-1330).
- src/usr/local/pkg/pfblockerng/pfblockerng.inc -- $pfb['continents'] / $pfb['continent_list']
  (~:160-171): the FROZEN structural bindings (alias prefixes, config-section roots).

REALITY OVERRIDE (standing rule)
Line numbers and code-state claims in this prompt were written before implementation; the live
tree and any prior RESULTS/ file override this prompt on any conflict -- verify each claim before
acting, and flag contradictions loudly in the handoff. The ADR's measured facts came from a real
licensed dataset on the maintainer's box; if the live code disagrees, the CODE wins and you
ESCALATE rather than patch around it.

ACTION PLAN (ordered)
1. Re-derive the surface from source (do NOT trust this prompt's line numbers): every artifact
   `ugc` writes -- the per-ISO cc/*.txt files, the per-continent .txt files, geoip_isos, the
   Top_Spammers .info file, and the 10 generated pages. Record the CURRENT file:line of each writer.
2. Capture the golden output. Off-appliance (tests/php/) where the code can be driven directly;
   the live-VM smoke (tests/smoke/) is the fallback for anything only `ugc` on a box can produce.
   Seed with the committed MaxMind test corpus (tests/smoke/fixtures/GeoLite2-Country*) so the
   capture is reproducible and credential-free.
3. The corpus has NO continent-level row (issue #1246) and therefore cannot exercise the
   AA ASIA/EUROPE UNDEFINED buckets. Add ONE continent-level Locations row + a Blocks row that
   references it as a documented local addendum to the fixtures (regenerated by
   scripts/update-geoip-fixtures.py; state clearly in fixtures/README.md that MaxMind's corpus
   carries none and why we add one). Capture the buckets' output too -- this is the coverage #1246
   asks for, and Phase 3 cannot prove D2 without it.
4. Pin the oracle with assertions that would FAIL on a regression: exact member sets, exact option
   text, exact counts -- never mere existence.

CONSTRAINTS
- Do NOT touch src/. Any production-file change in the diff is a phase failure.
- Fixtures follow tests/smoke/fixtures/README.md conventions; the GeoIP corpus's real public IP
  space is the documented exception (a GeoIP database can only classify addresses it knows).
- No new dependency.

VERIFICATION (executed -- paste the output, never "reasoned through")
- `python3 -m pytest` / `vendor/bin/phpunit` (whichever hosts the oracle) -- paste the summary.
- `scripts/agent/run-gates.sh --diff origin/devel` -- paste the verdict.
- The live-VM leg for anything captured on a box: `scripts/local-smoke.sh --marker smoke --filter
  <expr>` (PFB_BOXES from the owner). Never claim the smoke "needs CI".
- Prove the oracle is NON-VACUOUS: mutate one country's name/membership in the fixture, show the
  oracle goes RED, revert, show GREEN. Paste both runs.

HANDOFF
Write .ADRs/ADR_64_GeoIP_Truth_Table/RESULTS/01_Results.txt with the fixed fields from
CLAUDE.md "THE HANDOFF": Verdict; What changed (files + one-line why each + commit hash); Gates
(exact commands + pasted output tails); the oracle's non-vacuity proof (the mutation run: RED with the mutation, GREEN reverted); Coverage matrix (every row of this prompt's
ACTION PLAN ticked, or an explicit justified deferral); Deviations / judgment calls (or "none");
Carry-forward for the next phase.
