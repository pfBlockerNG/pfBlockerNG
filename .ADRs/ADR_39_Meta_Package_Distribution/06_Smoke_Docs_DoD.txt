================================================================================
PHASE 6 PROMPT — Smoke + docs + Definition of Done
Target model: Claude Sonnet 4.6
ADR: .ADRs/ADR_39_Meta_Package_Distribution/ADR.md  (read §4, §6 Phase 6, §7 DoD)
================================================================================

ROLE
Phase 6 of ADR-39 — prove the new path end-to-end and reconcile the docs. Touches
tests/smoke/ + docs (CLAUDE.md/README/ADR-20). Normal worktree + PR flow for the smoke; the ADR-20
amendment + CLAUDE.md/README are docs (land per the docs flow, but bundle with the smoke PR if it
is the same branch — CI skips docs-only paths anyway).

WHY THIS PHASE EXISTS
The ADR flips to Accepted on green automated coverage (CLAUDE.md "ADR acceptance"). The repo-marker
smoke must prove the meta-package install path works, self-heals on upgrade, and handles EOL — the
real behaviour the Worker used to (mis)handle.

REQUIRED READING
- FIRST, read RESULTS/05_Results.txt — the Worker/routing surface is gone. If missing, STOP.
- .ADRs/ADR_39_Meta_Package_Distribution/ADR.md  (§4 acceptance; §7 DoD + manual checklist + reject)
- tests/smoke/test_repo_install.py — the `repo` marker flow (install-from-our-repo, cross-repo
  precedence, `pkg upgrade`); `tests/smoke/_matrix.py` for per-leg ABI/varver.
- CLAUDE.md — "Self-hosted pkg repository (ADR-17)" + "Cloudflare routing Worker (ADR-20)" blocks;
  README.md install section; ADR-20 ADR.md status block.

ACTION PLAN
1. Smoke: extend the `repo`-marker suite to install via the META-PACKAGE (write initial conf with
   add-repo.sh → `pkg install pfblockerng-repo` → assert the conf now points at the box's correct
   `<varver>/<arch>` → `pkg update` accepts the catalog → `pkg install pfBlockerNG` pulls our build,
   priority 100). Derive the expected varver/arch from `_matrix.py` (never hardcode CE/Plus).
2. Self-heal case: assert BEFORE (conf for varver A) then simulate an upgrade/reinstall of the
   meta-package under a changed detected varver (mock the detection inputs) and assert the conf is
   REWRITTEN to varver B — green proves the post-install caused the change (before-and-after mandate).
3. EOL/route-only case: a box whose `<varver>` is a retained-but-EOL subtree (ADR-27) still gets a
   working conf pointing at its frozen catalog.
4. Docs: update CLAUDE.md (replace the "Cloudflare routing Worker" block with the meta-package +
   on-box detection model; update the ADR-17/20 self-hosted-repo description); update README install
   steps; add an amendment to ADR-20's status: "Superseded by ADR-39 — the UA-routed Worker +
   routing.json are retired in favour of on-box detection + the pfblockerng-repo meta-package;
   ADR-20's variant catalog tree is retained." Flip ADR-39 Status → Accepted only when §7 is met.

CONSTRAINTS
- Smoke domains/IPs per CLAUDE.md (unique_domain, RFC 5737/3849; never RFC 6761 / HSTS names).
- Probe on-box; the first response after reload is authoritative.
- Keep the matrix-derived topology (no hardcoded editions).

VERIFICATION
- `python -m pytest -m repo tests/smoke/test_repo_install.py` design-validates off-box where
  possible; the live legs run on the CE+Plus fan-out.
- markdownlint clean on touched docs.
- §7 DoD checklist satisfied; reject criteria not triggered.

EXPECTED RESULT
Green repo-marker smoke proving meta-package install + self-heal + EOL; docs reconciled; ADR-20
marked superseded; ADR-39 Status → Accepted.

COMMIT (single)
    test: prove meta-package install + self-heal; retire Worker docs (ADR-39)
Push to the branch; open a PR per the normal flow.

HANDOFF — write `RESULTS/06_Results.txt`
Record: the smoke cases added + their assertions; the doc edits; the ADR-20 supersede amendment;
the DoD status; and the final acceptance verdict.
