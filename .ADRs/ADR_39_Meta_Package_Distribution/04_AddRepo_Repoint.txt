================================================================================
PHASE 4 PROMPT — Repoint add-repo.sh + install the meta-package (boundary change)
Target model: Claude Sonnet 4.6
ADR: .ADRs/ADR_39_Meta_Package_Distribution/ADR.md  (read §2 Conf shape + contract items 1,2,4)
================================================================================

ROLE
Phase 4 of ADR-39 — the client boundary change. ARCHITECTURALLY SIGNIFICANT: the conf shape moves
from a Worker URL with `${ABI}` to a fully resolved direct-Pages path. Touches scripts/ + tests/.
Normal worktree + PR flow.

WHY THIS PHASE EXISTS
With no Worker, the conf can no longer rely on the Worker to map `${ABI}` → `<varver>/<arch>`. The
bootstrap must resolve the subtree on-box (Phase-1 helper) and write a literal direct URL, then
install the meta-package that maintains it thereafter.

REQUIRED READING
- FIRST, read RESULTS/03_Results.txt — the meta-package is now in every catalog (so
  `pkg install pfblockerng-repo` resolves). If missing, STOP.
- .ADRs/ADR_39_Meta_Package_Distribution/ADR.md  (§2 Conf shape; §1.3 byte-identity)
- scripts/add-repo.sh — `DEFAULT_BASE_URL` (~:66), `print_conf()` (~:140-156), the `pkg update`+
  rquery verify tail (~:175-201), `--nightly` / `--base-url` flags.
- scripts/build-repo.sh (`--print-conf`) and scripts/build-repo-portable.py (`--print-conf`) — the
  two other generators that MUST stay byte-identical.
- tests/test_add_repo_conf.py — the byte-identity + per-field + channel-non-clobber assertions.
- RESULTS/01_Results.txt — the detection helper interface.

ACTION PLAN (tests FIRST — flip the expected URL, watch them fail, then implement)
1. Update `tests/test_add_repo_conf.py` to expect the RESOLVED url
   (`https://pfblockerng.github.io/pkg/<channel>/<varver>/<arch>`, no `${ABI}`) for sample boxes,
   keeping every other field identical; keep the three-generator byte-identity assertion. Assert the
   OLD Worker URL is gone (before-state) and the new URL present (after).
2. `DEFAULT_BASE_URL` → `https://pfblockerng.github.io/pkg`. Source the Phase-1 helper; build the
   `url:` from `<base>/<channel>/<varver>/<arch>`. Keep `--base-url` (forks/staging) and `--nightly`.
3. Make all THREE `--print-conf` generators emit the identical resolved conf (they need the same
   varver/arch inputs — decide how `--print-conf` is parameterised for a target box vs the live box;
   keep them in lockstep).
4. After writing the initial conf + `pkg update`, `pkg install pfblockerng-repo` (the front-end that
   re-runs detection on upgrade). Keep the rquery verify tail (proves the catalog loaded).
5. Idempotence + channel non-clobber preserved (re-run → identical; release vs nightly confs coexist).

CONSTRAINTS
- POSIX sh; quote; LC_ALL=C on compares. No `${ABI}` in the emitted url.
- Byte-identity across the three generators is mandatory — a drift is a silent break.
- Do NOT delete the Worker here (Phase 5); this phase only repoints the client.

VERIFICATION
- `python -m pytest tests/test_add_repo_conf.py` → green (resolved URL, byte-identity, idempotence,
  non-clobber; old URL asserted absent).
- `sh -n` + `shellcheck` on all touched scripts → clean.
- `scripts/check_url_encoding.py` → clean (no naked interpolation regressions).
- diff-read: the three `--print-conf` outputs are byte-identical for the same target box.

EXPECTED RESULT
`add-repo.sh` writes a direct resolved conf via local detection and installs the meta-package; all
three generators agree byte-for-byte; the Worker URL is gone from the client path.

COMMIT (single)
    dev: point add-repo.sh at the direct Pages catalog and install the meta-package
Push to the branch; open a PR per the normal flow.

HANDOFF — write `RESULTS/04_Results.txt`
Record: the new DEFAULT_BASE_URL + resolved url format; how `--print-conf` is parameterised per
box; the three-generator lockstep; the meta-package install step; and the go-ahead for Phase 5
(Worker retirement) — note that the client no longer depends on the Worker.
