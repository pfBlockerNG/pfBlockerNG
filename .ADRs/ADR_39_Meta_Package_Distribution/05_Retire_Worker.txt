================================================================================
PHASE 5 PROMPT — Retire the Cloudflare Worker + routing.json
Target model: Claude Sonnet 4.6
ADR: .ADRs/ADR_39_Meta_Package_Distribution/ADR.md  (read §2 Worker + §1.3 "one consumer")
================================================================================

ROLE
Phase 5 of ADR-39 — delete the now-dead routing layer. Pure removal; behaviour-preserving for the
(now meta-package-driven) install path. Touches scripts/, CI, tests/, and `pfBlockerNG/pkg`'s
publish.yml. Normal worktree + PR flow. Do this ONLY after Phase 4 (client no longer needs the
Worker).

WHY THIS PHASE EXISTS
After Phase 4, no client uses the Worker, and routing.json has exactly one consumer (the Worker).
Both, plus their CI and tests, are dead weight and must go so the codebase reflects reality.

REQUIRED READING
- FIRST, read RESULTS/04_Results.txt — confirms the client path no longer depends on the Worker. If
  missing, STOP.
- .ADRs/ADR_39_Meta_Package_Distribution/ADR.md  (§2 Worker row; §3)
- scripts/worker/ (src/index.js, test/router.test.js) — the entire dir is deleted.
- .github/workflows/deploy-worker.yml; the `worker-tests` job in .github/workflows/test.yml; the
  `deploy-worker` job in .github/workflows/release.yml.
- scripts/build-repo-portable.py — `generate_routing_json` (~:407), `_ua_pattern` (~:552),
  `_dedup_routes` (~:575), and the `routing.json` write in the matrix builder (~:943).
- /home/user/pkg/.github/workflows/publish.yml — the routing.json generate/deploy step.
- tests/test_build_repo_portable.py — the routing/UA/dedup tests (~:695-1010).
- The `SMOKE_WORKER_LIVE` Case-4 leg `test_routing_url_delivers_variant_catalog` in
  tests/smoke/test_repo_install.py.

ACTION PLAN
1. Delete `scripts/worker/` entirely; `.github/workflows/deploy-worker.yml`; the `worker-tests` job
   (test.yml) and `deploy-worker` job (release.yml).
2. Remove `generate_routing_json`, `_ua_pattern`, `_dedup_routes`, the `--generate-routing-json`/
   routing CLI surface, and the routing.json write from `build-repo-portable.py`. Remove the
   routing.json generate/deploy step from `pfBlockerNG/pkg`'s publish.yml.
3. Delete the now-dead tests: `router.test.js`, and the routing/UA/dedup tests in
   `test_build_repo_portable.py`. KEEP `test_catalog_name_from_version` and the tree-layout tests.
4. Remove the `SMOKE_WORKER_LIVE`/Case-4 leg and any `SMOKE_WORKER_LIVE` references.
5. Grep gate: no dangling references to `worker`, `routing.json`, `workers.dev`, `_ua_pattern`,
   `generate_routing_json`, `SMOKE_WORKER_LIVE` anywhere except this ADR + ADR-20's superseded note
   (handled in Phase 6). Clean any stragglers (docs done in Phase 6).

CONSTRAINTS
- Removal only — do NOT change the catalog tree or the client conf.
- Leave the ADR-20 status amendment + CLAUDE.md/README edits to Phase 6 (docs phase), but ensure no
  CODE/CI references survive here.

VERIFICATION
- `python -m pytest` → green with the routing tests removed (no import/collection errors).
- `node --test` worker suite no longer exists; `test.yml` has no `worker-tests` job.
- yaml lint on touched workflows → clean.
- grep gate: zero code/CI references to the deleted Worker/routing surface.

EXPECTED RESULT
The Worker, routing.json, and all their CI + tests are gone; the suite is green; the only remaining
mentions are the ADRs (to be reconciled in Phase 6).

COMMIT (single)
    ci: retire the routing Worker and routing.json (replaced by on-box detection)
Push to the branch; open a PR per the normal flow.

HANDOFF — write `RESULTS/05_Results.txt`
Record: everything deleted (files, jobs, functions, tests, smoke leg); the grep-gate result; any
stragglers; and the go-ahead for Phase 6 (smoke + docs + DoD).
