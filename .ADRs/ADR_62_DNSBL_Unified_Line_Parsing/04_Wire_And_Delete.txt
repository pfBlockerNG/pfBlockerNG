================================================================================
PHASE 4 PROMPT — Wire PHP to raw-passthrough + delete the ABP classifier (behaviour-changing)
Target model: Sonnet 5 implementer under an orchestrator gate (xhigh adversarial review)
ADR: .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/ADR.md   (read §1.1, §1.2, §2 Decision + Semantics, §6 P4)
================================================================================

ROLE
You are the IMPLEMENTER. Work in the per-ADR worktree on branch
adr/62-dnsbl-unified-line-parsing. One commit, pushed to the branch. Implement yourself; NEVER
spawn sub-agents. BEHAVIOUR-CHANGING phase, gated by the Phase-1 byte-identity corpus staying
green. Follow CLAUDE.md (tabs, uppercase TRUE/FALSE, PFBL-01 sniff, str_* over preg_*).

WHY THIS PHASE EXISTS
With the Python dispatcher proven (Phase 3), PHP no longer needs to classify feeds as ABP or to
extract plain-feed domains itself. This phase makes PHP pass raw domain lines to the Python domain
path and DELETES the feed-level ABP classifier ($easylist, pfb_dnsbl_is_abp_header, the
$validate_header block), collapsing the doubled verbatim paths (ADR §1.1). The Phase-1 corpus is
the fail-before/pass-after gate: the new PHP path must reproduce the golden domain+IP set; a red is
a real regression.

REQUIRED READING
- FIRST, read the prior phase's handoff:
  .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/RESULTS/03_Results.txt — confirm parity was PROVEN (not
  BLOCKED). If Phase 3 returned a §7 reject signal, STOP: this phase does not proceed until the
  orchestrator resolves scope (Option B / reject). If the handoff is missing, STOP and report.
- .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/ADR.md (§1.1/§1.2 the classifier + manifest boundary,
  §2 Decision 1/3/4 + Semantics #1–#7, §7).
- src/usr/local/pkg/pfblockerng/pfblockerng.inc — the $validate_header block (~16283-16294),
  pfb_dnsbl_is_abp_header (~6547), the $easylist branches (~16317/16324/16572), pfb_feed_manifest_row
  (~6946) + the format='abp'/'plain' logic (~6990) + the .abp marker sites (~16182/16810), the
  Phase-2 helpers (collector + control-line predicate).
- RESULTS/01–03 for current file:line and the proven dispatcher contract.
- docs/misc/architecture-notes.md "DNSBL/ABP pipeline"; tests/php/README.md.

REALITY OVERRIDE (standing rule)
Line numbers/code-state predate implementation and earlier phases; RESULTS/01–03 and the live tree
override this prompt on any conflict — verify each claim before acting, and flag contradictions
loudly in the handoff.

ACTION PLAN (ordered)
1. Route domain lines to the Python path: PHP writes RAW domain lines to the per-feed staging/.raw
   (as ABP feeds already do, ADR §1.2) instead of PHP-extracting bare domains for plain feeds. Keep
   the IP-scan pass (Phase-2 collector, ADR §1.4) and CSV column extraction (out of scope, ADR §2).
2. Apply the universal control-line skip: use the Phase-2 pfb_dnsbl_is_skippable_control_line in the
   IP-scan pass so '!'/'//'/'[…]'-non-IPv6 lines are skipped everywhere; unbracket/collect IPv6
   FIRST so a bracketed IPv6 literal is an IP, never a skipped comment (ADR Semantics #3).
3. DELETE the classifier: remove the $validate_header block (~16283-16294), pfb_dnsbl_is_abp_header
   (~6547), and the $easylist variable + its three branches (~16317/16324/16572), collapsing the two
   verbatim-write paths into one raw passthrough.
4. Migrate format_hint: the domain feed's hint collapses to a single "domain" hint (CSV hints stay);
   ensure the .abp on-disk marker's routing role is preserved or cleanly retired (ADR Semantics #7)
   — a reused (not re-downloaded) feed must route the same as before.

CONSTRAINTS
- The Phase-1 byte-identity corpus (domain set AND DNSBLIP IP set) MUST stay byte-identical — that
  is the acceptance gate, not "looks right".
- Do NOT touch the CSV switch beyond what $easylist removal forces; do NOT touch the generic IP-list
  loop; do NOT alter parse_abp semantics.
- Clean the diff (CLAUDE.md): remove the now-dead code you deleted from, not around, no reformatting.

VERIFICATION (all must pass)
- Canonical gates: php -l touched files; vendor/bin/phpunit; vendor/bin/phpstan;
  vendor/bin/phpcs --standard=phpcs.xml.dist src/; python3 -m pytest (the parity oracle).
- Fail-before/pass-after via the corpus: the Phase-1 oracle is GREEN on the new path; then, in a
  scratch checkout of the PRE-change loop, confirm the oracle still described today's output (it was
  green there too — this is behaviour-preserving-by-construction, so the evidence is: same golden
  output both sides). If the new path changes ANY corpus row, that is a red regression — investigate,
  do not adjust the golden.
- grep proof (ADR Requirement 2): `grep -rn "easylist\|pfb_dnsbl_is_abp_header\|validate_header" src/`
  returns nothing (paste it).
- Semantics #2/#3 IP tests: bracketed-IPv6 collects as IP; non-IPv6 '[…]' skipped; ABP-anchored IP
  still collected.

EXPECTED RESULT
PHP passes raw domain lines to Python; the ABP classifier + header sniff are gone; corpus byte-
identical; IP set unchanged.

COMMIT (single)
    pfblockerng: retire DNSBL feed-level ABP classification; raw-passthrough to Python (ADR-62 P4)
Push directly to adr/62-dnsbl-unified-line-parsing; open a PR only if the direct push is rejected.

HANDOFF — write .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/RESULTS/04_Results.txt with the fixed
fields: verdict; files + why + commit; gates with pasted output; the corpus byte-identity evidence
(before/after golden equality); the grep-clean proof; the Semantics #2/#3 test results; deviations
(or "none"); carry-forward for Phase 5.
