================================================================================
PHASE 6 PROMPT — Docs, delta release notes, AUTOMATED smoke rows
Target model: Sonnet 5 implementer under an orchestrator gate (xhigh adversarial review)
ADR: .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/ADR.md   (read §5, §7 in full, §6 P6)
================================================================================

ROLE
You are the IMPLEMENTER. Work in the per-ADR worktree on branch
adr/62-dnsbl-unified-line-parsing. One commit, pushed to the branch. Implement yourself; NEVER
spawn sub-agents. Docs + smoke-test phase — no production-code behaviour change (the suites stay
green as the oracle that nothing shifted; the new smoke tests are additive). Docs use normal
professional grammar (CLAUDE.md documentation exception). Follow CLAUDE.md.

WHY THIS PHASE EXISTS
The as-built system (retired feed-level ABP classification; per-line shape capture; single Python
parse authority) must be documented for the next maintainer, and — per CLAUDE.md "ADR acceptance —
automated tests, not a manual sign-off" — the ADR flips to Accepted on green AUTOMATED coverage on
the ADR-04 live-VM CE+Plus smoke fan-out (tests/smoke/ DOES run a real pfSense VM in CI; there is
no "manual owner checklist" gate). This phase writes the architecture note, records the ADR §2
delta table where release notes are drafted, adds the §7 smoke rows as tests, and updates the ADR
Status.

REQUIRED READING
- FIRST, read the prior phase's handoff:
  .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/RESULTS/05_Results.txt — confirm perf within threshold
  and the tree clean. If missing, STOP and report.
- .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/ADR.md (§2 delta table, §5 constraints, §7 DoD + smoke
  rows + reject criteria) and RESULTS/01–05 for the as-built facts.
- docs/misc/architecture-notes.md "DNSBL/ABP pipeline" — the section to update.
- CLAUDE.md "Smoke tests (ADR-04)" + tests/smoke/ + tests/smoke/fixtures/README.md — the fan-out,
  fixture conventions (inert data, unique_domain(), block shapes, probe ON-BOX, chroot paths),
  and how a new feed case is added (mock_feeds.feed_url("<name>") + fixtures/README.md row).

REALITY OVERRIDE (standing rule)
Line numbers/code-state predate implementation; RESULTS/01–05 and the live tree override this prompt
— verify each as-built claim (what was actually deleted, the final helper names, the format_hint
outcome) against the RESULTS files before writing docs, and flag contradictions in the handoff.

ACTION PLAN (ordered)
1. Add/extend the AUTOMATED smoke coverage so every ADR §7 row (1–7) runs in the CE+Plus fan-out:
   fixture feeds under tests/smoke/fixtures/ (inert data; fixtures/README.md updated) + cases in
   the smoke feed suite (mock_feeds). Rows: plain hosts; ABP-content feed with NO .abp marker
   (||/@@/header/[Adblock]/regex/element-hiding); mixed plain feed (D2); bracketed-IPv6 vs
   [Adblock]; one CSV type; IDN; reused old-dialect .txt + a TLD-enabled update run. Each asserts
   a falsifiable observable (block shape per the list's logging, IP-alias membership, resolution
   for @@ names) — never HTTP 200 / "no error" alone. A row genuinely impossible in the harness is
   recorded in the ADR as a documented out-of-CI limitation with its manual command — never
   silently dropped.
2. Update docs/misc/architecture-notes.md "DNSBL/ABP pipeline": the retired classifier
   ($easylist / header sniff / $validate_header / .abp marker gone), the per-line shape-capture
   boundary (one PHP predicate at the loop + manifest writer; the mirrored Python routing), the
   IPv6-bracket carve-out, and the TLD-pass unconditional skip. Mirror the neighbouring entries'
   style/length. Cross-reference this ADR.
3. Record the ADR §2 delta table (D1–D5) in the ADR's Status section as the user-facing behaviour
   changes to carry into the next release's notes (the release-notes pipeline reads the commit
   range; the ADR is the canonical statement).
4. Update this ADR's Status line to the implemented state (e.g. "Implemented (pending smoke
   fan-out)" or "Accepted" if the fan-out already ran green — CLAUDE.md "ADR acceptance") with the
   honest as-built summary from RESULTS/01–05, including any DONE-WITH-DEVIATION carried by a
   phase.
5. Verify the phase prompts still lint (they are tracked docs):
   python3 scripts/check_phase_prompts.py .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/[0-9]*.txt

CONSTRAINTS
- No production-code behaviour change. Markdown passes markdownlint. Smoke fixtures are inert
  (RFC 5737/3849, uuid-*.com; never RFC 6761 TLDs or HSTS-preload names).
- The ADR flips to Accepted on green automated CE+Plus fan-out coverage of rows 1–7 — do NOT
  invent a manual sign-off gate, and do NOT flip it before that evidence exists.

VERIFICATION (all must pass)
- npx markdownlint-cli2 on the touched .md files (architecture-notes + ADR.md + fixtures README).
- python3 scripts/check_phase_prompts.py on all ADR-62 phase prompts (the /spec-lint gate) — clean.
- python3 -m pytest + the PHP suite stay green (nothing production changed; this proves it — the
  existing suites are the behaviour-preserving oracle for this phase).
- The new smoke cases are runnable: paste the local collection proof
  (python3 -m pytest tests/smoke --collect-only -k "<new case names>") and dispatch (or locally
  run via scripts/local-smoke.sh) the impacted smoke scope; paste the outcome or the exact
  dispatch command as DEFERRED evidence for the orchestrator.
- diff-read: the architecture note matches the as-built RESULTS, not this prompt's assumptions.

EXPECTED RESULT
architecture-notes updated; delta table recorded; §7 rows exist as automated smoke tests wired
into the CE+Plus fan-out; ADR Status reflects the evidence state; phase prompts lint clean.

COMMIT (single)
    docs+tests: document ADR-62 per-line parse authority; add smoke fan-out rows (ADR-62 P6)
Push directly to adr/62-dnsbl-unified-line-parsing; open a PR only if the direct push is rejected.

HANDOFF — write .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/RESULTS/06_Results.txt with the fixed
fields: verdict; files + why + commit; gates with pasted output (markdownlint + check_phase_prompts
+ suites green + smoke collection/dispatch evidence); confirmation the architecture note matches
the as-built; the final ADR Status set; deviations (or "none"); carry-forward (the smoke fan-out
acceptance evidence, if still pending).
