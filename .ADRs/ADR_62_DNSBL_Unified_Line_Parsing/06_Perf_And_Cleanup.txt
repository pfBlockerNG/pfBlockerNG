================================================================================
PHASE 6 PROMPT — Perf validation + dead-code cleanup
Target model: Sonnet 5 implementer under an orchestrator gate (xhigh adversarial review)
ADR: .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/ADR.md   (read §3 risks, §7 reject criterion 2, §6 P6)
================================================================================

ROLE
You are the IMPLEMENTER. Work in the per-ADR worktree on branch
adr/62-dnsbl-unified-line-parsing. One commit, pushed to the branch. Implement yourself; NEVER
spawn sub-agents. Follow CLAUDE.md.
Blast-radius note for the gate: dead-code deletion + measurement only — zero intended behaviour
change; the Phase-1 corpus stays byte-identical, deltas included.

WHY THIS PHASE EXISTS
The corrected design keeps the plain hot path (PHP extraction + Python parse()) and only adds
per-line prefix/shape checks, so the EXPECTED perf delta is ~0 — but "expected" is not evidence
(CLAUDE.md). ADR §7 reject criterion 2 makes it falsifiable: this phase measures a 1M-line hosts
feed against the kill-threshold and removes any code the Phase-4/5 diffs left dead.

REQUIRED READING
- FIRST, read the prior phase's handoff:
  .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/RESULTS/05_Results.txt — confirm the classifier is gone
  and the corpus is green (D1 flipped, D2–D5 held). If missing, STOP and report.
- .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/ADR.md (§3 Negative/risks, §7 reject criterion 2).
- src/usr/local/pkg/pfblockerng/pfb_unbound.py — the extended routing + build() path; the reload
  cost path.
- docs/misc/external-process-waits.md IF you launch any timed subprocess for the benchmark.
- CLAUDE.md (no fixed-time waits for concurrency, issue #456 — a benchmark timer is measurement,
  not coordination; keep it a measurement, no sleep-to-synchronise).

REALITY OVERRIDE (standing rule)
Line numbers/code-state predate implementation and earlier phases; RESULTS/01–05 and the live tree
override this prompt — verify before acting, flag contradictions in the handoff.

ACTION PLAN (ordered)
1. Build a reproducible benchmark: a synthetic large hosts feed (>= 1,000,000 inert lines,
   uuid-*.com / RFC 5737 IPs; include a realistic sprinkle of comment and ||-anchored lines) run
   through (a) origin/devel and (b) this branch — BOTH halves: the PHP parse loop's extraction
   (whatever surface Phase 1 established) and the Python parse+build. Measure wall-clock and peak
   RSS of the Python loader. State the methodology (machine, warm/cold, repeats, how you isolate
   the parse phase) in the handoff — a number with no methodology is not evidence.
2. Compare to the kill-threshold (ADR §7 criterion 2: > 25% wall-clock OR > 25% peak-RSS
   regression on the 1M-line parse+build). If exceeded: STOP and report BLOCKED with the numbers;
   do not paper over it.
3. Remove dead code the Phase-4/5 diffs orphaned (helpers only reachable from the deleted
   classifier, now-unused branches). Keep the diff to genuine dead code — no opportunistic
   reformatting. This step is behaviour-preserving: the corpus oracle is the gate.

CONSTRAINTS
- The benchmark is a dev/CI harness (bare `python3` under scripts/ is fine — it names the developer
  interpreter, CLAUDE.md); it does NOT ship to the appliance and must not add an appliance-side
  Python dependency.
- The Phase-1 corpus stays green, deltas included (dead-code removal changes no output).

VERIFICATION (all must pass; benchmark numbers are DATA, recorded not "passed")
- Canonical gates: python3 -m pytest; ruff check .; ruff format --check .; mypy tests/; and, if any
  *.php/*.inc touched by cleanup, php -l + phpunit + phpstan + phpcs --standard=phpcs.xml.dist src/.
- The Phase-1 byte-identity corpus oracle stays GREEN.
- Benchmark executed; wall-clock + RSS for devel vs branch pasted into the handoff with methodology;
  verdict against the kill-threshold stated explicitly (PASS / REJECT-criterion-2-triggered).
- diff-read: every removed symbol confirmed unreachable (grep it is gone) before deletion.

EXPECTED RESULT
A recorded perf comparison within threshold (or an evidenced reject signal), and a tree free of the
classifier's dead code.

COMMIT (single)
    pfblockerng: perf-validate DNSBL per-line capture; drop dead classifier code (ADR-62 P6)
Push directly to adr/62-dnsbl-unified-line-parsing; open a PR only if the direct push is rejected.

HANDOFF — write .ADRs/ADR_62_DNSBL_Unified_Line_Parsing/RESULTS/06_Results.txt with the fixed
fields: verdict; files + why + commit; gates with pasted output; the benchmark methodology + numbers
(devel vs branch, wall-clock + RSS) + the threshold verdict; the corpus-still-green confirmation; the
list of removed dead symbols with grep-gone proof; deviations (or "none"); carry-forward for Phase 7.
