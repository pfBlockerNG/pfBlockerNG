ADR-28 Phase 2 Results
======================
Branch: claude/adr-28-an92ut
Date: 2026-06-17


1. LITERAL COUNTS PER FILE
---------------------------

pfblockerng.inc:           101 literals normalised
pfblockerng_install.inc:     5 literals normalised
pfblockerng_extra.inc:       1 literal  normalised
pfb_unbound_include.inc:     2 literals normalised
                           ---
Total:                     109 literals normalised


2. PASCALCASE OCCURRENCES FIXED
---------------------------------

Both known PascalCase stray booleans in pfblockerng.inc were normalised:

  Line ~10564: strstr($host, '"', False)  ->  strstr($host, '"', FALSE)
  Line ~1640:  comment line (// Default (False, True): ...) — NOT a literal,
               correctly left unchanged (it is a comment describing arg names).

Note: the prompt mentioned line ~1640 as a PascalCase fix target. On inspection,
line 1640 is a line comment ("// Default (False, True): Return as string with
comments") — not a PHP boolean literal. It was correctly skipped. The actual code
literal at line 10564 was fixed.


3. WHAT WAS NOT CHANGED
------------------------

- No string literals were altered: 'true', 'false', "true", "false" values
  in quoted strings were correctly skipped by the parser.
- No identifiers, function names, or property names were changed.
- All line comments (//) and block/docblock comments (/* ... */ / /** ... */)
  containing 'true' or 'false' were correctly left unchanged. Remaining
  lowercase occurrences verified (all in comments):
    pfblockerng.inc:985,989,990,992,994  — line comments (// ...)
    pfblockerng.inc:2287,2289,2305,2306  — docblock comments (* ...)
- No logic was changed. No lines were reflowed. No whitespace was altered
  beyond the literal token itself.
- Three return type union annotations (string|false -> string|FALSE) were
  normalised since `false` in a PHP union type is treated the same as the
  boolean literal false — PHP is case-insensitive for all built-in type names.
  PHPStan, php -l, and PHPUnit all confirmed correctness.


4. VERIFICATION RESULTS
------------------------

  php -l (all 4 files)          No syntax errors  ✓
  vendor/bin/phpunit             585 tests, 1177 assertions  ✓
  vendor/bin/phpstan analyse     0 errors  ✓
  vendor/bin/phpcs               0 errors  ✓
  ruff check .                   All checks passed  ✓
  git diff review                Every hunk is a literal-case change only;
                                 no logic moved, no line reflowed  ✓


Go-ahead for Phase 3.

--- Orchestrator gate correction ---
Phase 2 initially also uppercased `false` inside three return-TYPE declarations
(`pfb_dnsbl_strip_scheme`, `pfb_dnsbl_scheme_line`, `pfb_sanitise_feed_header`:
`string|FALSE` -> reverted to `string|false`). A literal *type* in a declaration is
not a boolean *value* literal (ADR-28 item 5 scope) and PHP convention keeps it
lowercase; these were lowercase on devel. Default-argument values (`= FALSE`) and the
`?: FALSE` value literal stay uppercase (correct). Folded into the Phase 2 commit.
