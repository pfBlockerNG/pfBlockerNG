================================================================================
PHASE 4 PROMPT — GeoIP-aggregate (separate opt-in, same mechanism)
Target model: Claude Opus 4.8
ADR: .ADRs/ADR_11_Uber_Aliases/ADR.md   (read §2 "What it produces" + contract FIRST)
================================================================================

ROLE
You are on the `adr/11` branch performing Phase 4 of ADR-11. Apply the Phase-2/3 union
mechanism to the GeoIP **block** continents, behind its OWN opt-in toggle (default OFF),
producing pfB_GeoAggregate_{v4,v6}. ADDITIVE; isolated from the Deny aggregate. Work
inline on `adr/11`, one commit, push directly (PR only if rejected).

WHY THIS PHASE EXISTS
The maintainer asked for GeoIP to have its OWN uber-alias, separate from the Deny one
(GeoIP continents are huge → kept opt-in + isolated so its cost is contained and it
doesn't bloat the Deny aggregate).

REQUIRED READING (inspect before editing)
- FIRST, read the prior phase's handoff:
  .ADRs/ADR_11_Uber_Aliases/RESULTS/03_Results.txt — the wiring + registration pattern
  to reuse. If missing, STOP and report.
- .ADRs/ADR_11_Uber_Aliases/ADR.md (§2, contract).
- src/usr/local/pkg/pfblockerng/pfblockerng.inc — the GeoIP continent aliases
  ($pfb['continents'] :141-149, continent_list :152), the GeoIP cidr_aggregate call
  site (:8664), how continent block aliases are enumerated + their member files; the
  Phase-3 Deny-aggregate code to mirror.
- src/usr/local/pkg/pfblockerng/pfblockerng.sh — the `aggregate` action (Phase 2),
  cidr_aggregate (:268).
- CLAUDE.md.

ACTION PLAN (ordered)
1. SETTING: add a separate "Create aggregate GeoIP alias" toggle (default OFF),
   independent of the Deny toggle.
2. MEMBER SET: a helper that enumerates the **enabled, block-action** GeoIP continents/
   countries for the family (respecting the user's continent/country selections), and
   resolves their member files. Excludes non-block GeoIP entries.
3. WIRE + REGISTER: reuse the Phase-2 `aggregate` action + Phase-3 registration to emit
   pfB_GeoAggregate_{v4,v6} as Native urltable aliases (no firewall rule), in-pass,
   never-empty consumer file; teardown on toggle-off.
4. ISOLATION: the GeoIP aggregate shares NO state with the Deny aggregate; either can be
   on/off independently.

CONSTRAINTS
- ADDITIVE; Native (no rule); both toggles default OFF.
- Reuse Phase-2/3 code paths — do not fork the aggregation logic. PHP tabs/8.3; POSIX
  sh; lint-clean. Do NOT touch Python/tests.

VERIFICATION (must all pass before the phase is done)
- python -m pytest → green/unchanged.
- php -l + PHPStan + ShellCheck → clean.
- diff-read: GeoIP member set = block continents only; registration mirrors Phase 3;
  independent toggle; no firewall rule.

EXPECTED RESULT
With its toggle on, pfB_GeoAggregate_{v4,v6} build from the GeoIP block continents via
the same mechanism; independent of and isolated from the Deny aggregate.

COMMIT (single)
    pfblockerng: ADR-11 P4 add GeoIP aggregate alias (opt-in, Native) (ADR-11 P4)
Push directly to `adr/11`; open a PR only if the direct push is rejected. PR body via
--body-file.

HANDOFF — write `.ADRs/ADR_11_Uber_Aliases/RESULTS/04_Results.txt` (plain .txt)
State: the GeoIP toggle; the block-continent member-set helper; confirmation it reuses
Phase-2/3 paths + stays isolated from the Deny aggregate; the go-ahead for Phase 5.
