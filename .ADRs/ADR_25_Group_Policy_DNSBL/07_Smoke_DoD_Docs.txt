================================================================================
PHASE 7 PROMPT — Smoke (multi-source) + Definition of Done + docs
Target model: Sonnet 5 implementer under an orchestrator gate (xhigh adversarial review)
ADR: .ADRs/ADR_25_Group_Policy_DNSBL/ADR.md   (read SS0, SS2.1, SS7 first)
================================================================================

REVISED 2026-07-04: semantics are the SS2.1 ladder (default + exceptions, Client Groups +
bindings); the source-bound harness capability was ALREADY BUILT by ADR-55 Phase 4
(client_source(ip)) — REUSE it, do not build a second mechanism.

ROLE
Implementer in the per-ADR worktree on branch adr/25-group-policy-dnsbl. Phase 7 proves the
feature end-to-end on a live VM (the part CI can do), codifies the maintainer manual smoke
for anything CI can't, and updates docs. One commit. No sub-agents.

PRE-FLIGHT
Read RESULTS/06_Results.txt AND 05_/04_/01_. Missing => STOP and report.

REQUIRED READING
- ADR SS7 (DoD + reject criteria — you are closing them), SS2.1 (ladder cases to prove).
- tests/smoke/ — conftest.py (smoke_vm), helpers.py (unique_domain, drill probe,
  ensure_dnsbl_vip, config injection), the ADR-55 Phase-4 client_source(ip) fixture + its
  RESULTS notes (limits of the source-binding mechanism).
- RESULTS/01_Results.txt — the cache scheme to verify live (the bleed must NOT escape).
- docs/misc/architecture-notes.md — DNSBL pipeline section to extend.
- CLAUDE.md — Smoke tests (on-box probe, unique_domain, block shapes, no localhost
  exemption, first-response-authoritative), Updating documentation.

ACTION PLAN (ordered)
1. REUSE client_source(ip) for source-bound DNS probes (drill -I <src> from the second
   guest address). If a ladder case cannot be driven through it, document why and push that
   case to the manual checklist — never silently skip.
2. Smoke cases (each asserts the BEFORE state — no bindings => resolves — THEN the after):
   - policy_only group + Block binding for CG 1 only: name BLOCKED from CG 1's source,
     RESOLVES from a non-member source.
   - Warn binding: name RESOLVES from the CG's source AND a DNSBL report/log line is
     written (would-be block recorded).
   - Bypass binding on a default-enforcing group: CG's source resolves a name every other
     client gets blocked on (assert both directions).
   - CG allow_domains beats a Block binding for that CG only.
   - Most-permissive: a source in two CGs (Bypass + Block on the same group) resolves.
   - Rung-5 default: an unmatched source gets the default-enforcing group's block shape.
   - Legacy migration: pre-set pfb_gp/pfb_gp_bypass_list config -> Legacy_Bypass CG; those
     IPs skip DNSBL end-to-end (post-P6 single path).
   - SCHEDULE (issue #384): a Block binding with a short pfSense Schedule window blocks a
     listed name INSIDE the window and the same source resolves it OUTSIDE (drive a real
     boundary or pick a just-elapsed/just-future window) -> proves the chroot evaluates the
     right wall-clock window (SS2.4 TZ trap).
   - CACHE-BLEED (the Phase-1 scheme live): a pass/warn source warming a listed divergent
     name does NOT let a blocked source escape; an off-window resolution does NOT let an
     on-window query escape; a uniform (non-divergent) name IS served from cache (assert
     the positive too — divergence gating must not silently disable all caching).
   Use helpers.unique_domain() (uuid-*.com); never RFC 6761 TLDs or HSTS-preload names.
3. Manual maintainer checklist (RESULTS + reflected in ADR SS7) for any case CI cannot
   cover.
4. Docs: extend docs/misc/architecture-notes.md with the group-policy section (trilogy map,
   bitmask model, gpClientGroups, the ladder, divergence-gated caching + the warm-name
   flush, schedule evaluation + chroot TZ handling, legacy handover); update README.md if
   workflow changed; stubs for any new pfSense fns; PFBL-01 scopeFunctions current.
5. Re-confirm DoD + reject criteria against the Phase-1 numbers; if the live scheme misses
   the kill-threshold, record it and recommend reduce/reject per ADR SS7. Update the ADR
   Status per the repo's ADR-acceptance rule (green CE+Plus fan-out => Accepted).

CONSTRAINTS
- Smoke probes ON-BOX; first response after reload is authoritative (assert, don't loop).
- Every new assert prints expected-vs-actual (probe result, ruleset slice) — no bare bools.
- stdlib-only python; POSIX sh; markdownlint clean.

VERIFICATION
- python -m pytest green (non-smoke suite); the smoke cases green on the LIVE-VM FAN-OUT
  (CE + Plus — the acceptance gate; a single leg is not the gate); out-of-CI residuals
  recorded in ADR SS7 — never silently SKIPped as if covered; ruff / mypy tests/ /
  markdownlint clean.

COMMIT (single)
    pfblockerng: add group-policy smoke + docs; close ADR-25 DoD (ADR-25 P7)
Push directly to adr/25-group-policy-dnsbl.

HANDOFF — write .ADRs/ADR_25_Group_Policy_DNSBL/RESULTS/07_Results.txt (plain .txt)
Record: which DoD items are CI-covered vs manual; the source-binding reuse (or why not);
the live cache-scheme result vs the kill-threshold; doc/stub updates; the final
accept/reduce/reject recommendation.
