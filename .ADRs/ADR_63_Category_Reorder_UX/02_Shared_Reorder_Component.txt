================================================================================
PHASE 2 PROMPT — Shared client-side reorder component (drag + anchor-click, staged, dormant)
Target model: Claude Sonnet 5 implementer under an orchestrator gate (xhigh adversarial review)
ADR: .ADRs/ADR_63_Category_Reorder_UX/ADR.md   (read §1.2, §1.3, §2 Decision 1-2 first)
================================================================================

ROLE
You are the IMPLEMENTER. Work in the per-ADR worktree on branch adr/63-category-reorder-ux.
One commit, pushed to the branch. Implement yourself; do NOT spawn sub-agents.
Blast-radius note for the gate: PRODUCTION-DORMANT. This phase builds the shared JS component
and proves it in isolation (a scratch/fixture HTML page); it is not wired into either live page
(Phase 3/4) and cannot change any production page's behaviour yet.

WHY THIS PHASE EXISTS
ADR §1.2/§1.3 (read before writing any code): pfSense core's firewall_rules.php pattern (drag +
anchor-click, both staged client-side, one Save write) is worth copying; its helper functions
(reindex_rules/fr_toggle/save_separators, core js/pfSenseHelpers.js) are NOT reusable — verified
hard-coded to firewall_rules.php's own DOM ids. pfBlockerNG's OWN pfblockerng_category.php
already implements the correct persistence shape (stable row id + one order array, computed at
Save time, never renumbering other per-row fields) — this phase's component must follow THAT
shape, not port reindex_rules()'s "relabel every attribute of the moved row on every move"
approach, which does not fit pfblockerng_category_edit.php's several independently-named
per-row fields (state-{id}/format-{id}/url-{id}/header-{id}, ADR §1.3).

REQUIRED READING
- FIRST, read the prior phase's handoff: .ADRs/ADR_63_Category_Reorder_UX/RESULTS/01_Results.txt
  — confirm the stable row-id convention and coverage matrix it recorded. If missing, Phase 1
  is not complete; STOP and report.
- .ADRs/ADR_63_Category_Reorder_UX/ADR.md §1.2, §1.3, §2 Decision 1-2, Consequences (JS-harness
  risk paragraph).
- src/usr/local/www/pfblockerng/pfblockerng_category.php — the EXISTING sortable init (~:687)
  and save_new_changes()'s ids-serialize (~:611-664) as the shape to generalise, not replace.
- src/usr/local/www/pfblockerng/pfBlockerNG.js (or wherever the page's existing custom JS
  lives — grep `<script src=` in both pages) — confirm the right file/location for a new shared
  helper (a new js/ file vs an addition to the existing one; your call, document why).
- CLAUDE.md "Code standards" www/ JS row of the code-quality table (const enums/booleans,
  String.prototype over RegExp where applicable).

REALITY OVERRIDE (standing rule)
Line numbers and code-state claims in this prompt were written before this phase ran; Phase 1's
RESULTS and the live tree override this prompt on any conflict — verify each claim before
acting, flag contradictions loudly in the handoff.

ACTION PLAN
1. Design the component's public surface: a function/module taking a table selector, the
   per-row stable-id attribute/prefix, the checkbox name pattern, and the hidden order-field
   name to populate at Save time. It must expose: drag init (thin wrapper generalising
   category.php's existing `.sortable()` options), anchor-click wiring (checkbox + per-row
   anchor button; click = move checked rows before the clicked row, shift-click = after —
   mirrors firewall_rules.php's UX, re-implemented against pfBlockerNG's own row markup), a
   `dirty` flag + Save-button enable/disable, and an order-read function returning the current
   DOM order of stable row ids (the ids[]-equivalent, computed only at Save time — never
   renumbering any other per-row field name).
2. Read `system/webgui/roworderdragging` SERVER-SIDE (PHP, via config_path_enabled) — this
   phase only needs to accept a boolean parameter deciding whether the component's drag init
   runs; the actual read-and-pass-in wiring happens in Phase 3/4. Do not read this pref
   client-side.
3. Prove the component in isolation: a scratch/fixture HTML page (not either production page)
   with a handful of rows, exercised via Playwright — drag, anchor-click single-row,
   anchor-click multiple-rows-straddling-the-anchor (the client-side analogue of the #1145 edge
   case: with the whole move happening in the DOM before any server round-trip, there is no
   server-side vote-tally to get wrong, but the CLIENT-side insertBefore/insertAfter sequencing
   for multiple checked rows must still produce a well-defined, correct final order — assert it
   for at least: all-checked-rows-before-target, all-after, and straddling both sides).
4. Do NOT wire this into pfblockerng_category.php or pfblockerng_category_edit.php in this
   phase — confirm via diff that neither production PHP page changes.

CONSTRAINTS
- No new front-end dependency (jQuery + jQuery UI only, already loaded platform-wide) — do not
  add a package manager, bundler, or JS test framework.
- The component must NOT read or write any other per-row named field (state-{id}, format-{id},
  etc.) — order is the ONLY thing it computes; that is the whole point of the design (ADR §1.3).
- `const`/boolean literals lowercase per CLAUDE.md's www/ JS row.
- Comment budget <=3 lines per CLAUDE.md.

VERIFICATION (all must pass)
- php -l on any touched PHP (if the pref-read helper lands as a small PHP function this phase —
  optional, your call whether to add it now or in Phase 3/4).
- The scratch-page Playwright proof: drag, anchor-click single, anchor-click multi-straddle —
  each asserting the exact final DOM order, pasted output.
- Diff review: zero changes to pfblockerng_category.php or pfblockerng_category_edit.php.
- No coverage-theater: each scratch-page assertion must be shown to fail on a deliberately wrong
  component behaviour (e.g. temporarily swap insertBefore for insertAfter in the anchor-click
  path, confirm the multi-straddle test goes red, then revert) — paste the red-run proof.

EXPECTED RESULT
A production-dormant, isolated-proven shared JS reorder component ready for Phase 3
(pfblockerng_category.php, lower risk) to wire up first.

COMMIT (single)
    www: add shared client-side drag+anchor-click reorder component (ADR-63 P2)
Push directly to adr/63-category-reorder-ux; open a PR only if the direct push is rejected.

HANDOFF — write .ADRs/ADR_63_Category_Reorder_UX/RESULTS/02_Results.txt with the fixed fields:
verdict; what changed + commit; gates with pasted output; the scratch-page proof (all three
scenarios, including the red-run for the multi-straddle case); the component's exact public
surface (function names/params) for Phase 3/4 to call; deviations (or "none"); carry-forward.
