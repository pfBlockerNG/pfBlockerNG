ADR-63 PHASE 5 -- Docs, release-notes delta, DoD smoke rows (final phase)
HANDOFF (orchestrator, docs phase handled directly per CLAUDE.md)

== VERDICT ==
DONE

== WHAT CHANGED ==
- docs/misc/architecture-notes.md: new "Category list reorder UX (ADR-63)" section --
  the shared client-side component (pfb_reorder_init/pfb_reorder_read_order), the TWO
  persistence shapes it feeds (category.php ids[]-at-Save AJAX; category_edit positional
  field names via core renumber() after every staged move) and why they differ (§1.3),
  the read-only roworderdragging reuse, the injected-control paint-layer fix, and the
  retired symbols ($Lmove/$Xmove/$pre/$disable_move/move_anchor) named for a future grep;
  #1152 (category.php ids[] hostile-order) + #1201 (geoip persist no-op) called out as
  deliberate out-of-scope. Commit 0266a13c.
- .ADRs/ADR_63_Category_Reorder_UX/ADR.md: Status flipped Proposed -> Accepted, citing the
  green CE+Plus fan-out (run 29170634245).

== CE+PLUS LIVE-VM FAN-OUT (ADR §7 acceptance) ==
Dispatched: gh workflow run ui-tests.yml --ref adr/63-category-reorder-ux
  -f scope=full -f tier=browser -f pytest_filter="reorder or sort_mode"
Run: https://github.com/pfBlockerNG/pfBlockerNG/actions/runs/29170634245 -> completed/success
  - build-pkg (2.8, pfsense-ce)      => success
  - build-pkg (26.03, pfsense-plus)  => success
  - browser / ce-2.8   (live VM)     => success -- 43 passed, 364 deselected (3:41)
  - browser / plus-26.03 (live VM)   => success -- 43 passed, 364 deselected (3:41)
Both editions green over the §7 reorder rows. Per CLAUDE.md "ADR acceptance", green
automated CE+Plus coverage = Accepted (no manual sign-off gate).

== §7 SMOKE ROW COVERAGE (rows 1-6, all automated, selected by one -k filter) ==
1. category.php drag-reorder + Save + reload persists -> test_reorder_drag_only_then_save_
   persists_dom_order (test_browser_category.py) [P3]
2. category.php anchor-click single + straddle + Save -> test_reorder_anchor_click_single/
   straddle_then_save_persists_dom_order (test_browser_category.py) [P3]
3. category_edit no-sort drag AND anchor + Save + reload, no full reload per move ->
   test_reorder_drag_only / anchor_click_* / mixed_* (test_browser_category_edit.py) [P4]
4. category_edit no-sort staged reorder + add + delete composed + Save ->
   test_reorder_composition_add_delete_field_edit_then_save_persists_all [P4]
5. category_edit sort=='sort' no manual UI + auto-sort unchanged ->
   test_sort_mode_renders_no_manual_reorder_ui_and_preserves_auto_sort [P4] +
   CategoryEditAutoSortTest (PHPUnit oracle) [P1]
6. roworderdragging SET anchor-only + Save round-trip both pages ->
   test_reorder_roworderdragging_set_disables_drag_anchor_click_still_persists (both
   test_browser_category.py [P3] and test_browser_category_edit.py [P4]); loud teardown
   restores the prior value.
All six pre-existed from Phases 1/3/4; this phase confirmed the full set runs together
under `-k "reorder or sort_mode"` on the CE+Plus fan-out. No row retrofitted to serve
double duty; no new production src/ change.

== GATES ==
- npx markdownlint-cli2 docs/misc/architecture-notes.md -> Summary: 0 error(s)
- python3 -m pytest (full suite) -> 2666 passed, 5 skipped (no regression from the doc pass)
- CE+Plus fan-out browser legs -> both success (above)

== DEVIATIONS ==
1. Docs phase handled directly by the orchestrator (higher model), not via the phase-step
   Sonnet implementer -- CLAUDE.md: "the higher model ... always handles docs / config /
   settings / skills directly." Not a recorded deviation from the delegation contract.
2. Release-notes delta NOT written as a committed docs/release-notes/<tag>.md file. alpha.9
   is already tagged; the next tag is not yet cut, and under the pipeline's COMMITTED-FILE
   PRECEDENCE a partial file carrying only this ADR's delta would SUPPRESS the GitHub-Models
   notes for every other change in that release. The user-facing delta (touch/mobile reorder
   for both category pages, #1147; instant staged category-edit reorder replacing
   one-reload-per-move) rides the commit messages the Models pipeline consumes, and is
   documented in the ADR + architecture-notes. Creating the file at release-cut time is the
   safe path.

== CARRY-FORWARD ==
None -- final phase. ADR-63 is Accepted; all five phases landed with RESULTS + Gate records.
