================================================================================
PHASE 4 PROMPT — Wire pfblockerng_category_edit.php + retire $Lmove/$Xmove
Target model: Claude Sonnet 5 implementer under an orchestrator gate (xhigh adversarial review)
ADR: .ADRs/ADR_63_Category_Reorder_UX/ADR.md   (read §1.1, §1.3, §1.5, §1.7, §2 Decision 4-5,
§4 Requirements 2-4 first)
================================================================================

ROLE
You are the IMPLEMENTER. Work in the per-ADR worktree on branch adr/63-category-reorder-ux.
One commit, pushed to the branch. Implement yourself; do NOT spawn sub-agents.
Blast-radius note for the gate: BEHAVIOUR-CHANGING, the ADR's core delta. Delta budget: (a) the
manual (no-sort) reorder INPUT method changes from full-page-POST-per-move to staged
drag+anchor-click with the page's normal single Save (mobile/touch gains a working method;
desktop loses one-reload-per-move); (b) the persisted order equals the VISUAL order at Save
(Semantics #1) — explicitly NOT bug-compatible with the retired placement math, whose #1145
multi-row-straddle bug is LIVE on devel (ADR §1.1); (c) sort=='sort' (Semantics #2) must NOT
move at all — zero delta, not even mechanism; (d) the save handler's POST surface is
byte-unchanged (Semantics #4) — no new fields, no new parsing.

WHY THIS PHASE EXISTS
The $Lmove/$Xmove/$pre server-side placement logic (pfblockerng_category_edit.php:928-983) is
the code class issues #1145 (LIVE bug — its fix PR #1150 was closed unmerged, superseded by
this ADR) and #1149 (closed, folded in) both live in. This phase deletes the class. There is
NO replacement server logic: pfSense core's renumber() (called by the component after every
staged move) keeps the per-row field names positional, and the page's EXISTING save loop
(:822-846) already persists whatever the field names say — the same channel client-side delete
persists through today (ADR §1.3).

REQUIRED READING
- FIRST, read the prior phases' handoffs:
  .ADRs/ADR_63_Category_Reorder_UX/RESULTS/03_Results.txt — exactly how roworderdragging and
  the component were wired (mirror it, do not reinvent); RESULTS/02_Results.txt — the
  component surface and the renumber-composition proof; RESULTS/01_Results.txt — the
  sort=='sort' oracle and the tombstone suites. If any is missing, STOP and report.
- .ADRs/ADR_63_Category_Reorder_UX/ADR.md §1.1, §1.3, §1.5, §1.7, §1.8, §2 Decision 4-5,
  Semantics #1-#4/#7, coverage matrix (mutation axis), §4 Requirements 2-4, §7 rows 3-6 +
  reject criteria.
- src/usr/local/www/pfblockerng/pfblockerng_category_edit.php — read ALL of these before
  editing: the POST capture (~:156-158), the #1106 array-reject loop + Lmove exemption
  (~:502-513), the per-field save loop + rowhelper prune (~:822-846), the reorder block
  (~:928-983), the $disable_move sites (~:80, ~:199, ~:938, ~:1759), the auto-sort branch
  (~:1096-1113, DO NOT TOUCH), the no-sort move_anchor UI + row-number gutter (~:1132-1141),
  the repeatable/addrow/deleterow markup (~:1130, ~:1226, ~:1341).
- src/usr/local/www/pfblockerng/pfBlockerNG.js — the disable_move block (~:148-158), deleted
  with the mechanism.
- tests/php/CategoryEditRowMoveTest.php + tests/smoke/ui/test_category_edit_row_move.py (being
  deleted — read fully first; their #1129 data-integrity intent must be demonstrably covered
  by this phase's new tests). NOTE: CategoryEditRowMoveTest's bootstrap eval-extracts the
  reorder block by regex and THROWS when the block is absent — the block deletion and this
  file's deletion MUST land in the same commit or the whole PHP suite errors (ADR §1.5).
- tests/php/CategoryEditPostGuardTest.php — STAYS; its R15 pins the Lmove array-exemption you
  are retiring. Update it test-first (see plan item 4).
- issue #1129 (gh issue view 1129 --comments) for the ORIGINAL data-integrity intent: a
  stale/hostile move request must never silently drop or duplicate a row.

REALITY OVERRIDE (standing rule)
Line numbers and code-state claims in this prompt predate this phase; RESULTS/01-03 and the
live tree override this prompt on any conflict — verify each claim before acting, flag
contradictions loudly in the handoff.

ACTION PLAN (ordered — new coverage proven BEFORE the old files go, all in one commit)
1. In the sort=='no-sort' branch ONLY, replace the old checkbox+anchor-submit UI (~:1132-1141)
   with the component's markup: checkbox + type="button" anchor per row, trailing-digit-
   suffixed names/ids (so core renumber() rewrites them consistently), keeping the row-number
   gutter element. The sort=='sort' branch and its rendering path are UNTOUCHED (zero lines).
2. Wire the Phase-2 component exactly as Phase 3 did (drag gated by roworderdragging, boolean
   passed server-side→JS), with onAfterMove = core renumber() + the gutter refresh (renumber
   the visible row numbers to match DOM positions). No order field is posted; the page's
   normal Save persists the staged state via the EXISTING save loop — confirm via diff that
   :822-846 and the whole isset($_POST['save']) branch are byte-unchanged.
3. DELETE the retired mechanism, every site (enumerated from git grep, ADR Requirement 3):
   the POST capture (~:156-158), the reorder block (~:928-983) including its
   Location-redirect-per-move, the Lmove exemption inside the array-reject loop (~:505-508,
   the loop itself STAYS and now rejects all array fields — update its comment), the
   $disable_move flag (~:80, ~:199, ~:938) and its JS export (~:1759), and the
   pfBlockerNG.js:148-158 disable block. Client-side staging works on a brand-new unsaved
   alias, so the disable_move carve-out has no successor (ADR §1.1).
4. Update tests/php/CategoryEditPostGuardTest.php R15 TEST-FIRST (this is a real behaviour
   change of the guard loop): author the new expectation — an array-valued 'Lmove' POST field
   is now REJECTED like any other array field — run it RED against the untouched code, freeze
   (record git hash-object), then make the production edit (item 3's exemption removal), rerun
   GREEN zero-edits. Paste both runs.
5. DELETE tests/php/CategoryEditRowMoveTest.php and tests/smoke/ui/test_category_edit_row_move.py
   in this same commit (see the bootstrap-throw note above). BEFORE deleting, record in the
   handoff the superset-of-intent mapping: each old test's #1129 data-integrity scenario →
   the new test that covers the equivalent guarantee (the composition tests below; the
   placement-math outputs themselves are intentionally retired, including the live #1145
   behaviour — do NOT try to reproduce them).
6. Tree-wide retirement sweep (the #1047 straggler class): git grep -n
   'Lmove\|Xmove\|disable_move' -- ':!.ADRs' must return ZERO hits; and grep -n
   '\$Lmove\|\$Xmove\|\$disable_move\|\$pre\b'
   src/usr/local/www/pfblockerng/pfblockerng_category_edit.php must return ZERO hits. Paste
   both.

CONSTRAINTS
- sort=='sort' rendering and behaviour: byte-identical, zero lines touched in that branch.
- NO new $_POST reads or parsing anywhere (Semantics #4) — consequently the PFBL-01
  RequirePfbFilter sniff scope list is untouched; verify, don't extend.
- Do not change the per-field save loop, the rowhelper prune, or the geoip-specific save
  logic — the server side of this page is pure deletion.
- Comment budget <=3 lines; PHP tabs; uppercase TRUE/FALSE; www/ JS conventions.
- Any test that sets roworderdragging restores it in a loud teardown (ADR §5).

VERIFICATION (all must pass)
- php -l; vendor/bin/phpunit (full suite); composer phpstan; composer phpcs --
  --standard=phpcs.xml.dist src/.
- Requirement 3: both retirement greps, zero hits, pasted.
- PHPUnit: the updated CategoryEditPostGuardTest green, with the R15 red→green proof pasted
  (red-time hash equal to the committed file's).
- Tier A (`ui_render`): the page renders 200 for both sort modes, no PHP errors.
- Tier B (`ui_browser`, Playwright), across the §2 gtype axis {ipv4, ipv6, geoip, dnsbl}:
  (a) sort=='no-sort': drag-only reorder + Save + reload — persisted order equals the visual
      order at Save.
  (b) sort=='no-sort': anchor-click-only (single row, then multiple rows straddling the
      anchor) + Save, NO drag — persisted order equals visual; assert NO page navigation
      happened per individual move (only on Save).
  (c) COMPOSITION (ADR Requirement 4 / §7 row 4): staged reorder + core add-row + core
      delete-row in ONE session, plus a per-row field edit, then Save — persisted row set,
      order, and the edited value all match the visual state; no row lost or duplicated.
  (d) sort=='sort': no manual-reorder UI renders and the auto-sorted order is byte-identical
      to the Phase-1 oracle (Semantics #2 — MUST show zero delta).
  (e) roworderdragging SET (restored on teardown): only anchor-click renders AND an
      anchor-click reorder + Save round-trip persists correctly.
- The gutter numbers match DOM positions after each staged move (assert within (a)/(b)).

EXPECTED RESULT
pfblockerng_category_edit.php's no-sort mode offers staged drag+anchor-click reorder committed
on the page's normal Save, with zero server-side placement math (the #1145 bug class and #1149
retired by deletion); sort=='sort' is byte-identical to before; the #1106 array-reject guard is
stricter (no exemptions).

COMMIT (single)
    www: replace category-edit row-move with staged client-side reorder (ADR-63 P4)
Push directly to adr/63-category-reorder-ux; open a PR only if the direct push is rejected.

HANDOFF — write .ADRs/ADR_63_Category_Reorder_UX/RESULTS/04_Results.txt with the fixed fields:
verdict; what changed + commit; gates with pasted output; the R15 red→green proof (both runs +
red-time hash); the five Tier-B scenario results (a-e), (d) explicitly showing ZERO delta; the
superset-of-intent mapping (old test scenario → new coverage) recorded before deletion; both
retirement greps' zero-hit output; deviations (or "none"); carry-forward for Phase 5 (which
issues/behaviours belong in release notes and architecture-notes — at minimum #1147, #1145,
#1149, #1152's deliberate exclusion, and the retired symbol names).
