---
paths:
  - "scripts/agent/**"
---
Soft routing backstop (#1439): for `scripts/agent/` changes, read `.agents/policy/delegation.md` section "Agent-ops scripts" and `.agents/policy/git.md` (bootstrap routing rows).
