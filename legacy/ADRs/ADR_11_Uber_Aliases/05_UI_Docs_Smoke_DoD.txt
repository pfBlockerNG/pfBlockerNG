================================================================================
PHASE 5 PROMPT — Settings UI + docs + benchmark + manual smoke + DoD
Target model: Claude Opus 4.8
ADR: .ADRs/ADR_11_Uber_Aliases/ADR.md   (read §4, §7 FIRST)
================================================================================

ROLE
You are on the `adr/11-uber-aliases` branch performing Phase 5 of ADR-11 — the closing
phase: finish the settings UI (the multi-select) + help text, document the feature,
re-run the cost benchmark on the branch, and finalise the Definition of Done + reject
criteria + the live-box manual smoke (CI cannot load pf tables or run HAProxy). Work
inline on the branch, one commit, push directly (PR only if rejected).

WHY THIS PHASE EXISTS
The multi-select needs clear UI + help; the feature's per-type semantics, the Permit
"reference-only" caveat, and the RAM/opt-in caveat + naming must be documented for the
next maintainer; and the manual smoke is the gate (no automated oracle for PHP/shell
alias building).

REQUIRED READING (inspect before editing)
- FIRST, read the prior phase's handoff:
  .ADRs/ADR_11_Uber_Aliases/RESULTS/04_Results.txt (and 03). If missing, STOP.
- Also RESULTS/01_Results.txt — the cost numbers + build strategy + RAM caveat to
  document, and the bench script (scripts/misc/) to re-run.
- .ADRs/ADR_11_Uber_Aliases/ADR.md (§4, §7).
- The pfBlockerNG settings page(s) under src/usr/local/www/pfblockerng/ + the settings
  schema; README.md + CLAUDE.md.

ACTION PLAN (ordered)
1. UI: finalise the opt-in MULTI-SELECT (Deny / Permit / Match / Native) on the settings
   page with clear help text — what it creates (pfB_<Type>_Aggregated_{v4,v6}), that the
   aggregates are Native (NO rule), the Permit/Match "reference IP-set only — does not
   permit/match anything by itself" caveat, that GeoIP folds in by each continent's
   action (no separate Geo alias), and the RAM caveat (each loaded table is wired kernel
   memory; enable only what you consume). Match neighbouring help-text style/length.
2. DOCS: README.md + CLAUDE.md — the feature, the fixed alias names + the per-type model,
   the opt-in/default-none + RAM note, the lockstep/in-pass (mtime-gated) build, and the
   ADR-12 hand-off (the never-empty consumer file + the Native aliases are what HAProxy
   will consume; freshness in ADR-12 is a pfBlockerNG-triggered graceful HAProxy reload,
   NOT a socket push). markdownlint clean.
3. BENCHMARK: re-run the Phase-1 union-cost benchmark on the branch; record the branch
   numbers vs the soft budget (note the per-type extension is bounded by the deny union).
4. DoD + SMOKE: finalise §7 — the reject/pivot criteria (cost-prohibitive → finer
   incremental; cannot-stay-additive → STOP) and the per-type live-box manual smoke
   checklist (none-selected no-op; Deny effective-set incl. DNSBLIP + Deny-GeoIP, suppressed
   absent; Permit/Match/Native dir unions; all-combinations additive; lockstep + mtime-gate;
   never-empty file; HAProxy referenceable; teardown). Status flips to Accepted only after
   the maintainer passes it.

CONSTRAINTS
- No new shipped behaviour beyond UI wiring + docs (the build logic landed in P2-P4).
- php -l + PHPStan + ShellCheck + markdownlint clean; python -m pytest untouched/green.
- Do NOT edit other ADRs' docs. Web UI help text: brief yet clear, match neighbours.

VERIFICATION (must all pass before the phase is done)
- python -m pytest → green/unchanged.
- php -l + PHPStan + ShellCheck + markdownlint → clean.
- The multi-select renders with help; README/CLAUDE.md describe the per-type feature +
  caveats; the branch benchmark number is recorded; §7 smoke checklist is complete +
  actionable.

EXPECTED RESULT
ADR-11 is implementation-complete with lint-clean code, documented settings + caveats,
a branch cost number, and a maintainer per-type smoke checklist — Proposed until the live
smoke passes.

COMMIT (single)
    docs: ADR-11 settings UI + per-type aggregate docs + DoD (ADR-11 P5)
Push directly to `adr/11-uber-aliases`; open a PR only if the direct push is rejected.
PR body via --body-file.

HANDOFF — write `.ADRs/ADR_11_Uber_Aliases/RESULTS/05_Results.txt` (plain .txt)
Final summary: the multi-select + help; the docs added; the branch benchmark vs budget;
the per-type manual-smoke checklist status; whether ADR-11 is ready to flip from Proposed
toward Accepted pending the maintainer's live smoke; and the explicit ADR-12 hand-off
(Native aggregates + never-empty files; reload-trigger freshness, no socket).
