================================================================================
PHASE 1 PROMPT — Golden oracles for both pages' CURRENT reorder behaviour
Target model: Claude Sonnet 5 implementer under an orchestrator gate (xhigh adversarial review)
ADR: .ADRs/ADR_63_Category_Reorder_UX/ADR.md   (read §1.1, §1.5, §1.8, §2 coverage matrix first)
================================================================================

ROLE
You are the IMPLEMENTER. Work in the per-ADR worktree on branch adr/63-category-reorder-ux.
One commit, pushed to the branch. Implement yourself; do NOT spawn sub-agents. This phase is
BEHAVIOUR-PRESERVING — it adds tests only, touches zero production code.
Blast-radius note for the gate: NONE. Test coverage only; no production file changes, no
persisted-behaviour change possible.

WHY THIS PHASE EXISTS
Later phases delete pfblockerng_category_edit.php's $Lmove/$Xmove reorder mechanism and change
how both pages' reorder UIs work. Before touching either, this phase captures TODAY's exact
behaviour: pfblockerng_category.php's ids[] AJAX order-save path has NO existing test coverage
(the module docstring of tests/smoke/ui/test_category.py mentions ids[] at :12/:22 but no test
posts an order — verify this yourself); pfblockerng_category_edit.php's sort=='sort' and
no-sort $Lmove/$Xmove behaviour IS covered (tests/php/CategoryEditRowMoveTest.php — 9 tests on
devel — and tests/smoke/ui/test_category_edit_row_move.py) and must be reconfirmed green as the
"before" tombstone. IMPORTANT CONTEXT: issue #1145's fix (PR #1150) was CLOSED UNMERGED — the
multi-row placement bug is LIVE on devel and those suites pin the live (buggy) behaviour. That
is fine: the whole mechanism is deleted in Phase 4; these oracles are a tombstone snapshot, not
a contract the new mechanism must reproduce.

REQUIRED READING
- .ADRs/ADR_63_Category_Reorder_UX/ADR.md (§1.1-§1.5, §1.8, §2 Decision + coverage matrix +
  Semantics #1-#7).
- src/usr/local/www/pfblockerng/pfblockerng_category.php — the "Save new Table order format
  (via AJAX)" block (~:277-306), save_new_changes() + the ids[] serialize (~:640-684), the
  sortable tr id convention (id="pfb_r{r_id}", ~:426).
- src/usr/local/www/pfblockerng/pfblockerng_category_edit.php — the sort/no-sort field
  (~:413, ~:1457-1463), the auto-sort branch (~:1096-1113), the no-sort move_anchor UI
  (~:1132-1141), the $Lmove/$Xmove reorder block (~:928-983).
- tests/php/CategoryEditRowMoveTest.php (existing oracle, do not modify — note its bootstrap
  eval-extracts the production block by regex; that pattern is also the house precedent for
  pinning inline page code, relevant to item 4 below).
- tests/smoke/ui/test_category_edit_row_move.py, tests/smoke/ui/test_category.py,
  tests/smoke/ui/test_browser_category.py, tests/smoke/ui/test_browser_category_edit.py (the
  existing Tier-B/Playwright harness conventions to reuse — do not invent a new harness).
- CLAUDE.md "Test coverage" (mandatory five), "Smoke tests" (Tier A/B rules, fixture rules —
  helpers.unique_domain(), inert data only).

REALITY OVERRIDE (standing rule)
Line numbers and code-state claims in this prompt were written before any phase ran; the live
tree and prior RESULTS override this prompt on any conflict — verify each claim before acting
on it, and flag contradictions loudly in the handoff.

ACTION PLAN
1. Re-derive the §2 coverage matrix from a fresh grep of both files' current code (gtype
   handling, sort field, row id conventions, repeatable/addrow/deleterow markup) — record any
   discrepancy from the ADR's matrix in the handoff.
2. Add Tier-B (ui_e2e or ui_browser, match the existing convention for HTTP-POST vs
   real-browser as appropriate) coverage for pfblockerng_category.php's ids[] AJAX order-save
   path: for each gtype in {ipv4, ipv6, geoip, dnsbl}, POST a WELL-FORMED crafted ids[]
   reflecting a specific new order (a permutation of the current rows — hostile/malformed
   orders are issue #1152's scope, deliberately NOT pinned here), reload, assert the persisted
   row order matches. Include a vacuity check (mutate one golden expected order — the oracle
   must go red; paste the red, restore, paste the green).
3. Confirm (do not modify) tests/php/CategoryEditRowMoveTest.php and
   tests/smoke/ui/test_category_edit_row_move.py are green on the current tree — paste the run
   in the handoff. These are the tombstone snapshot Phase 4 deletes.
4. Add one small oracle explicitly pinning pfblockerng_category_edit.php's sort=='sort' output
   (auto-order by header then state) for a mixed enabled/disabled, out-of-alphabetical-order
   fixture, across the same gtype axis — this is Semantics #2's byte-identity baseline every
   later phase must not move. PHPUnit via the CategoryEditRowMoveTest-style eval-extraction of
   the auto-sort block (~:1096-1113), or Tier-B — your call, document which and why (do NOT
   re-implement the sort logic in the test; that would pin a copy, not the code).

CONSTRAINTS
- Zero production-code changes in this phase. Test files only.
- Fixtures: inert data only (RFC 5737/3849 IPs, helpers.unique_domain() where a Tier-B test
  touches DNS-resolvable names) — never RFC 6761 TLDs or HSTS-preload names.
- Do not touch tests/php/CategoryEditRowMoveTest.php or test_category_edit_row_move.py's
  content — Phase 4 deletes them; this phase only re-runs them.
- New tests are intent-named (what "today's" behaviour is being pinned), not mechanics-named.

VERIFICATION (all must pass)
- Canonical gates for touched languages (PHP tests: php -l, vendor/bin/phpunit, composer
  phpstan, composer phpcs -- --standard=phpcs.xml.dist src/ if any src/ touched — none expected
  this phase; Python tests: python3 -m pytest, ruff check ., ruff format --check ., mypy tests/).
- New category.php ids[] oracle: green, with its vacuity check demonstrated (mutate → red,
  paste the failure; restore → green).
- Existing CategoryEditRowMoveTest.php + test_category_edit_row_move.py: green, unmodified,
  pasted output.
- New sort=='sort' oracle: green.
- No existing test file's assertions weakened.

EXPECTED RESULT
A complete, evidenced "before" snapshot of both pages' current reorder behaviour, including the
previously-uncovered category.php ids[] path, ready to gate every later phase's claim of
behaviour-preservation or intentional, evidenced delta.

COMMIT (single)
    tests: pin current category-list and category-edit reorder behaviour (ADR-63 P1)
Push directly to adr/63-category-reorder-ux; open a PR only if the direct push is rejected.

HANDOFF — write .ADRs/ADR_63_Category_Reorder_UX/RESULTS/01_Results.txt with the fixed fields
(CLAUDE.md "THE HANDOFF"): verdict; what changed + commit; gates with pasted output; the
re-derived coverage matrix (confirm or correct the ADR's); the oracles with pasted green (and
vacuity-red) output; deviations (or "none"); carry-forward for Phase 2 (in particular: the
exact row markup both pages render today — tr.sortable vs .form-group.repeatable — and the
field-naming pattern the component must respect).
