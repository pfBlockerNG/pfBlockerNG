================================================================================
PHASE 3 PROMPT — Wire pfblockerng_category.php (adds anchor-click; drag path preserved)
Target model: Claude Sonnet 5 implementer under an orchestrator gate (xhigh adversarial review)
ADR: .ADRs/ADR_63_Category_Reorder_UX/ADR.md   (read §1.1, §1.8, §2 Decision 2-3, §4
Requirements 5-6 first)
================================================================================

ROLE
You are the IMPLEMENTER. Work in the per-ADR worktree on branch adr/63-category-reorder-ux.
One commit, pushed to the branch. Implement yourself; do NOT spawn sub-agents.
Blast-radius note for the gate: BEHAVIOUR-CHANGING, scoped narrowly. This phase adds a new
input path (anchor-click) to a live page and swaps the client-side order-read implementation.
Delta budget: it may change how a user CAN produce a given row order (mobile/touch users gain a
working reorder method — implements #1147, closed as superseded by this ADR); it must NOT
change what order gets PERSISTED for a given final visual state reached via the EXISTING drag
path (Semantics #1) — the Phase-1 drag oracle must stay green, unmodified, byte-identical.

WHY THIS PHASE EXISTS
pfblockerng_category.php already has the correct persistence shape (stable pfb_r{id} + ids[]
order array, computed once at Save) — issue #1147 (mobile drag broken) is purely a missing
INPUT method. This is the lower-risk of the two pages (server-side byte-unchanged) and lands
before Phase 4 to prove the Phase-2 component against a real page first.

REQUIRED READING
- FIRST, read the prior phase's handoff:
  .ADRs/ADR_63_Category_Reorder_UX/RESULTS/02_Results.txt — the component's exact public
  surface (function/param names) you must call. If missing, Phase 2 is not complete; STOP and
  report. Also read RESULTS/01_Results.txt for the Phase-1 category.php ids[] oracle you must
  keep green.
- .ADRs/ADR_63_Category_Reorder_UX/ADR.md §1.1, §1.4 (geoip/gtype axis), §1.8, §2 Decision
  2-3, Semantics #1/#3/#6/#7, §4 Requirements 5-6.
- src/usr/local/www/pfblockerng/pfblockerng_category.php — the tr id="pfb_r{r_id}" convention
  (~:426), the existing sortable init (~:687), save_new_changes() + sortable('serialize')
  (~:640-684), the server-side ids[] handler (~:277-306, UNCHANGED by this phase — confirm via
  diff it stays byte-identical).
- tests/smoke/ui/test_browser_category.py (the existing Playwright harness to extend, not
  replace).
- CLAUDE.md "Smoke tests" Tier A/B rules.

REALITY OVERRIDE (standing rule)
Line numbers and code-state claims in this prompt predate this phase; RESULTS/01-02 and the
live tree override this prompt on any conflict — verify each claim before acting, flag
contradictions loudly in the handoff.

ACTION PLAN
1. Read `system/webgui/roworderdragging` via `config_path_enabled('system/webgui',
   'roworderdragging')` server-side; pass the resulting boolean into the page's JS (a small
   inline JS variable, matching the page's existing `var pagetype = ...;`-style convention) as
   the component's drag-enable parameter.
2. Render the anchor-click UI (checkbox + per-row type="button" anchor) using the component's
   markup contract from Phase 2's handoff, reusing the EXISTING pfb_r{r_id} stable id — do not
   introduce a second id scheme for the same row. Update the table header/colspans as the new
   column requires.
3. REPLACE the `sortable('serialize')` call in save_new_changes() with the component's
   order-read helper (same ids[] wire format — the server cannot tell the difference). This is
   NOT optional and NOT "alongside": sortable('serialize') throws when .sortable() was never
   initialised, which is exactly the roworderdragging-set mode (ADR §1.8). The SAME ids[]
   value must be produced whether the user dragged, anchor-clicked, or both before Save.
4. Do NOT gate, disable, or re-enable #btnsave — it is the page's general-purpose Save (it
   saves #iform field edits with zero reorder staged) and stays always-enabled (ADR §1.8).
5. Confirm via diff that the server-side ids[] handler (~:277-306) is byte-identical to
   origin/devel — this phase is client-only.

CONSTRAINTS
- No server-side PHP change beyond the single roworderdragging read (item 1) — the save
  handler itself is byte-unchanged (issue #1152's hostile-order gap stays open, untouched).
- Do not touch the geoip-specific per-row-field save branch (~:261) — unrelated to reorder.
- Comment budget <=3 lines; www/ JS conventions (lowercase booleans/const).
- Any test that sets roworderdragging on the VM restores the prior value in a teardown that
  fails loudly if the restore doesn't take (ADR §5) — a leaked SET breaks later drag tests.

VERIFICATION (all must pass)
- php -l on the touched file; composer phpstan; composer phpcs -- --standard=phpcs.xml.dist src/.
- Tier A (`ui_render`): the page renders 200, no PHP errors, both UI affordances present when
  roworderdragging is unset.
- Tier B (`ui_browser`, Playwright): across the §2 gtype axis {ipv4, ipv6, geoip, dnsbl} --
  (a) drag-only reorder + Save: the Phase-1 golden oracle, UNCHANGED and green (regression:
      same scenario, now exercised on a page that also offers anchor-click and reads order via
      the component helper instead of sortable('serialize')).
  (b) anchor-click-only reorder (single row, then multiple rows straddling the anchor target)
      + Save, with NO drag interaction: assert final persisted order matches DOM order at Save
      time (new coverage, proves #1147's scenario without simulating touch/drag).
  (c) mixed drag-then-anchor-click in the same session + Save: assert final order matches.
  (d) roworderdragging SET (restored on teardown): assert only anchor-click renders (no drag
      handles/sortable init) AND a full anchor-click reorder + Save + reload round-trip
      persists the new order — the render check alone would miss the serialize trap (§1.8).
  (e) a Save with field edits and ZERO reorder staged still saves those edits (Semantics #3 /
      the never-gate-Save rule).
- Diff review: server-side ids[] handler byte-identical to origin/devel.

EXPECTED RESULT
pfblockerng_category.php offers both drag and anchor-click reorder, gated by roworderdragging,
with identical persisted-order semantics regardless of input method; #1147's scenario works.

COMMIT (single)
    www: add anchor-click reorder to pfblockerng_category.php, implements #1147 (ADR-63 P3)
Push directly to adr/63-category-reorder-ux; open a PR only if the direct push is rejected.

HANDOFF — write .ADRs/ADR_63_Category_Reorder_UX/RESULTS/03_Results.txt with the fixed fields:
verdict; what changed + commit; gates with pasted output; the five Tier-B scenario results
(a-e) with pasted output; confirmation the server-side handler diff is empty; deviations (or
"none"); carry-forward for Phase 4 (in particular: exactly how roworderdragging was wired and
how the component was initialised, so Phase 4 mirrors it, not reinvents it).
