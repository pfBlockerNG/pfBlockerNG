================================================================================
PHASE 3 PROMPT — Live-VM smoke: canonical reject line per wired stage
Target model: Claude Sonnet 5
ADR: .ADRs/ADR_48_Download_Validation_Logging/ADR.md   (read §4, §6 Phase 3, §7)
================================================================================

Run /ponytail:ponytail full

ROLE
Branch: adr/48-download-validation-logging (off devel)
Phase 3 of 4 — TEST-ONLY phase (authors the ADR-04 smoke coverage; no src/
change). The live CE+Plus fan-out itself is dispatched by the ORCHESTRATOR
after this phase lands — you author tests that are correct by construction
and verifiable off-box (collection + lint), you do NOT dispatch workflows.
Work ENTIRELY inside the worktree (`git -C <worktree> ...`). One commit, push.

WHY THIS PHASE EXISTS
Phases 1-2 landed the canonical line + wiring. Acceptance (§7) needs live
proof: a forced rejection at each wired stage emits the canonical greppable
line into the pfB log on a REAL pfSense VM, and a healthy feed emits NO
spurious REJECT line.

REQUIRED READING
- FIRST the prior handoffs: RESULTS/01_Results.txt + RESULTS/02_Results.txt —
  the exact rendered canonical lines per site (grep targets). If missing -> STOP.
- CLAUDE.md — the ENTIRE "Smoke tests (ADR-04)" section (unique_domain, no
  RFC 6761 TLDs, on-box probing, expected-vs-actual diagnostics mandate) and
  Test coverage non-negotiables.
- tests/smoke/test_smoke_feeds.py — the ADR-16/44/45 mock-feed cases are the
  template: _MockFeedServer / mock_feeds.feed_url(), the existing
  corrupt-archive tests (ADR-45) and MIME tests (ADR-44), and the log-marker
  helper (count_log_marker or the current equivalent — read how existing
  tests assert pfB-log content and reuse it).
- tests/smoke/fixtures/README.md + the archive_* fixture corpus (ADR-45) —
  FreeBSD-verified corrupt/valid archives already exist; REUSE them, do not
  mint new corrupt archives (macOS/Linux tools disagree with FreeBSD
  file(1)/bsdtar — that burned ADR-45; see the README's on-box verdicts).
- tests/smoke/helpers.py — unique_domain(), log helpers.

ACTION PLAN (ordered)

1. Map each wired stage to a forcing fixture (extend test_smoke_feeds.py):
   - stage=structural: an existing ADR-45 corrupt-archive fixture feed (e.g.
     the corrupt gzip or zip). The feed already gets rejected today — the NEW
     assertion is the canonical line.
   - stage=mime: a fixture whose file(1) mime is outside the allowlist and
     not octet-recoverable (reuse the ADR-44/45 reject fixture if one exists;
     else add an inert binary fixture per the fixtures rules + README entry).
   - stage=inner: a VALID zip container whose EXTRACTED member fails the
     FILE_MIME re-run (zip wrapping an inert disallowed-mime blob), or a
     compressed-peek failure — pick whichever the existing fixture corpus
     can force reliably ON FREEBSD; verify your choice against the README
     verdicts, and document which inner sub-path (peek vs post-extraction)
     the fixture exercises.
2. Per stage, one test: configure the mock feed -> force update -> assert
   (a) the feed is rejected (alias/DNSBL absence — the existing pattern) AND
   (b) the pfB log gained a line matching
       'pfb_validate: REJECT feed=<hdr> stage=<stage>'
   with the stage token pinned. Grep the RENDERED (escaped) form from the
   02 handoff. Before-state per the coverage mandate: assert the marker
   count is 0 BEFORE the update, >0 after (no false pass from an earlier
   test's lines — count deltas, not absolutes).
3. Healthy-feed guard: after a successful update of a known-good fixture
   feed, assert ZERO NEW 'pfb_validate: REJECT' lines (delta again).
4. Diagnostics: every assertion prints expected-vs-actual (the log excerpt
   actually found) on failure — no bare booleans (CLAUDE.md mandate).
5. If a genuinely new fixture file is needed: inert bytes only, add the
   fixtures/README.md row with its FreeBSD verdict marked "expected —
   verify on-box in the fan-out" (you cannot run FreeBSD file(1) here).

TEST COVERAGE NOTES
- These smoke tests are the red->green proof for Phase 2's call-site wiring:
  on pre-ADR-48 code they FAIL (no canonical line) — state this in the test
  docstrings (tests document intent).
- Self-encapsulated: each test resets/derives its own baseline (delta
  counting); no ordering dependence on sibling tests.
- Marker: the module already carries the smoke marker — keep new tests in
  test_smoke_feeds.py so selective dispatch picks them up as changed-module.

VERIFICATION (off-box — the live run is the orchestrator's fan-out)
- python -m pytest tests/smoke/test_smoke_feeds.py --collect-only
  --override-ini="addopts=" -> new tests collected, no import errors
- python -m pytest (unit suite) -> green
- ruff check . && ruff format --check . -> clean
- mypy tests/ (pre-commit runs it on *.py) -> clean
- git diff --stat -> tests/smoke/* (+ fixtures/README.md, fixture files) only

SELF-REVIEW BEFORE THE HANDOFF
Read your own `git diff`. Confirm: one test per wired stage + the healthy
guard; delta-based log assertions with before-state; fixtures follow the
inert-data rules and reuse the ADR-45 corpus where possible; docstrings state
the red-on-pre-change intent; diagnostics print the actual log excerpt.

EXPECTED RESULT
- tests/smoke/test_smoke_feeds.py covers: canonical line on structural, mime,
  and inner rejects; zero spurious REJECT lines on a healthy feed.
- Everything collectable and lint-clean off-box; ready for the CE+Plus
  fan-out that gates Accepted.

COMMIT (single)
    tests/smoke: assert canonical pfb_validate REJECT line per wired stage (ADR-48 P3)
Push directly to adr/48-download-validation-logging.

HANDOFF — write RESULTS/03_Results.txt
Include:
- Test names + which stage/fixture each forces; which inner sub-path the
  inner test exercises and why the fixture is FreeBSD-reliable.
- Any NEW fixture files + their expected on-box verdicts to confirm in the
  fan-out.
- The exact pytest -k filter covering all new tests (the orchestrator's
  dispatch uses it).
- Collection/lint results; commit hash + push confirmation.
- Note for the orchestrator: fan-out green (CE + Plus) is the Accepted gate
  for Phases 1-3; Phase 4 lands after with its own smoke leg.
